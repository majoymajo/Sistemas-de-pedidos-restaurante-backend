# Matriz de Pruebas — HDU-03 a HDU-05

---

## HDU-03 — Validación de Estructura y Tipo de Datos con 400

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-03-001** | JSON con llave sin cerrar.Retorna→ 400. |  JSON con llave sin cerrar sin exponener stack traces internos (Jackson/Spring).Retorna→ 400.  | Un JSON malformado es error del cliente; El mensaje debe ser accionable sin revelar internals del parser. |
| **CT-03-002** | JSON con comilla sin cerrar.Retorna→ 400. | JSON con comilla sin cerrar y sin exponer nombres de clases internas.Retorna→ 400. | Si el cliente envió JSON inválid.El mensaje debe guiar sin filtrar detalles del parser. |
| **CT-03-003** | JSON con caracteres no escapados.Retorna→ 400. | JSON con caracteres no escapados Retorna→ 400 y debe ser capturado por el @ExceptionHandler| El ajuste confirma que este error es capturado por el @ExceptionHandler global y devuelve la respuesta ErrorResponse igual a cualquier otro 400, en lugar de una respuesta inconsistente o HTML. |
| **CT-03-004** | Cuerpo vacío `""` o sin body en POST /orders. Retorna→ 400. | Cuerpo vacío `""` o sin body en POST /orders. Retorna→ 400.Probar body `""` y request sin body por separado. | Sin payload no hay orden. El defecto debe distinguir "body vacío" de "body malformado". |
| **CT-03-005** | `tableId` como string `"mesa5"`. Retorna → 400. | `tableId` como string `"mesa5"`. Retorna → 400. Verificar que Jackson no convierta silenciosamente el string a número | Si el sistema acepta strings donde espera números sin quejarse, entonces el cliente nunca sabrá que está enviando datos mal tipados, lo que podría causar bugs difíciles de seguir. |
| **CT-03-006** | `tableId` como objeto `{}`.Retorna → 400. | `tableId` como objeto `{}`. → 400. Retorna → 400 con `ErrorResponse`. | Lo que se puede comprobar es que intentar pasar un objeto {} donde se espera un número simple falla inmediata, explícita y con el formato estándar de error, no con un 500 ni con HTML. |
| **CT-03-007** | `items` como string `"producto1,producto2"`. Retorna → 400. | `items` como string `"producto1,producto2"`. Retorna → 400. El error indica que `items` debe ser un array. | El servidor rechaza el CSV porque espera una array JSON y el error es suficientemente informativo como para que el cliente pueda corregir. |
| **CT-03-008** | productId como string en lugar de number. Retorna → 400. |  productId como string en lugar de number. Retorna → 400. El error indica que el tipo de dato no es el correcto. | El mensaje debe señalar el campo y el tipo esperado para que el cliente sepa que debe corregir . |
| **CT-03-009** | quantity como string en lugar de number. Retorna → 400. | quantity como string en lugar de number. Retorna → 400 indicando tipo incorrecto. | `quantity` debe ser numérico para cálculos, por eso el error debe ser preciso sobre el campo fallido. |
| **CT-03-010** | `quantity` como null. Retorna → 400 o 422. | `quantity` como null. Retorna → 400 o 422. El error menciona que `quantity` es requerida. | Si el cliente envía null, el servidor debe fallar diciendo "quantity es requerido", evitando así crear órdenes fantasmas con 0 unidades.|
| **CT-03-011** | Falta `tableId` en payload. Retorna → 400. | Falta `tableId` en payload. Retorna → 400. El error menciona que `tableId` es requerido. | Sin `tableId` no se sabe a dónde llevar el pedido, por eso el mensaje debe indicar el campo ausente. |
| **CT-03-012** | Falta items en payload. Retorna → 400. | Falta items en payload. Retorna → 400. El error menciona que items es requerido. | Una orden sin items es inválida, por eso el mensaje debe informar sobre un faltante total de items o una lista vacía. |
| **CT-03-013** | Falta `productId` en un item. Retorna → 400. | Falta `productId` en un item. Retorna → 400. El error indica que `productId` es requerido. | Sin `productId` no se sabe qué producto ordenar, por eso el mensaje debe indicar el campo faltante exacto dentro del arreglo. |
| **CT-03-014** | Falta `quantity` en un item. Retorna → 400 o 422. | Falta `quantity` en un item. Retorna → 400 o 422. El mensaje menciona que `quantity` es requerido | La falta de cantidad no debe asumirse nunca como un 1 por defecto, debe ser rechazada explícitamente obligando al cliente a definirla. |
| **CT-03-015** | `note` como número `123` en lugar de string. Retorna → 201. | Ningún ajuste adicional | - |
| **CT-03-016** | Payload con campo adicional `"extraField"`. Retorna → 201. | Payload con campo adicional `"extraField"`. Retorna → 201. Verificar que dicho campo extra es ignorado por completo. | Jackson ignora campos desconocidos por defecto. Este test confirma que la persistencia en DB y la respuesta hacia el cliente no filtran ni muestran esa basura extra. |
| **CT-03-017** | JSON válido con tipos correctos (flujo feliz). Retorna → 201. | JSON válido con tipos correctos. Retorna → 201 con un UUID v4 asignado. Verificar que se persistan bien todos sus campos. | Se asegura de que las operaciones ideales de venta realmente se almacenen como se deben. |
| **CT-03-018** | JSON válido con `note: null`. Retorna → 201. | JSON válido con `note: null`. Retorna → 201. Verificar que `note` se quede como un valor nulo de datos sin serializarse accidentalmente al string `"null"`. | Una nota nula es equivalente a sin nota, enviar un null directamente debe parsearse bien y debe cuidarse de que una librería lo acabe metiendo de forma textual en la DB. |
| **CT-03-019** | JSON con 5 items válidos. Retorna → 201. | JSON con 5 items válidos. Retorna → 201. Verificar que los 5 elementos existan tanto en persistencia como al retornar respuestas validativas. | Comprar varios elementos a la vez compone el escenario normal de todo negocio, ratificando la robustez serial del endpoint a listados. |
| **CT-03-020** | Distinción entre 400 y 422. Retorna → 400 / 422. | Diferenciar errores en el documento. Retorna → 400 para malformaciones nativas JSON y Retorna → 422 en caso de reglas lógicas del negocio violadas. | Si el cliente que consume los apis no conoce la naturaleza de su falla, no sabrá si arregla el json crudo en el frontend o arregla los limitantes del negocio. |
| **CT-03-021** | Arreglo limpio `items: []`. Retorna → 422. | Arreglo limpio `items: []`. Retorna → 422. El mensaje le indicará textualmente la obligación mínima "al menos un item requerido". | Pasarlo con formato vacío respeta a Java pero lógicamente arruina al restaurante, requerir datos not-null de esta forma impone sentido al ciclo de negocios. |

---

## HDU-04 — Desactivación Lógica de Productos con Auditoría

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-04-001** | Desactivar producto activo (id=1). Retorna → 200 OK. | Desactivar producto activo (id=1). Retorna → 200 OK con metadata auditable, verificando estricto uso del dato `updatedBy`. | Las desactivaciones impunes pierden rápidamente trazabilidad. Saber quien desactivó el artículo y hasta qué tiempo se lo desactivó ofrece una garantía formal de trazabilidad interesante en la gerencia.|
| **CT-04-002** | Desactivar producto activo (id=1). Retorna → 200 OK y verifica que fechas no cambian. | Desactivar producto activo (id=1). Retorna → 200 OK confirmando la inmutabilidad histórica del timestamp primario `createdAt`. | El timestamp funde trazabilidad a la base de datos. Si sobre-escribimos por actualizar arruinariamos la línea total temporal original de registro. |
| **CT-04-003** | Desactivar producto y verificar GET /menu no lo trae. Retorna → 200 OK. | Desactivar producto y correr listado. Retorna → 200 OK ratificando por el conteo la ausencia total de identificadores pasados al filtro. | Elementos extintos en visibilidad confunden ordenadores y cocineros posibilitando demandas inexistentes de comida, provocando así caídas en el servicio. |
| **CT-04-004** | Desactivar producto ya inactivo (id=2). Retorna → 200 OK. | Ningun cambio adicional |- |
| **CT-04-005** | Múltiples DELETE al mismo producto. Retorna → 200 OK. | Ningun cambio adicional | - |
| **CT-04-006** | DELETE a producto 999. Retorna → 404. | DELETE a producto 999. Retorna → 404 Not Found con mensaje que avisa estrictamente no se encontró índice exacto inyectado en path del objeto de fallback estándar. | Carencias de items irreales no quiebran reglas en un java sino al contrato de la api. |
| **CT-04-007** | DELETE al identificador -1. Retorna → 404. | DELETE al identificador -1. Retorna → 404 Not Found previniendo interacciones nocivas de un dígito incalculable. | Si no se verifica que los números enviados tengan sentido (por ejemplo, no aceptar un ID negativo), los errores pueden llegar a la base de datos e incluso eliminar datos incorrectos por error. |
| **CT-04-008** | DELETE con nulos y límite 0. Retorna → 404. | Ningun cambio adicional | - |
| **CT-04-009** | DELETE sin Header. Retorna → 401. | DELETE evadiendo todo el nivel Header Auth. Retorna → 401 Unauthorized sin ninguna posibilidad a borrar o acceder a transacciones. | Si no exigimos que la persona esté autenticada para poder eliminar algo, cualquier usuario malintencionado podría borrar la información desde internet. |
| **CT-04-010** | DELETE con firmamento dañado de token. Retorna → 401. | Tratar exclusiones asomando tokens desbordados. Retorna → 401 Unauthorized ocultándole por completo el diagnóstico de token viciado u otro a la contraparte del llamado. | Guardar total silencio contra ataques inhabilitadores neutraliza el mapeado directo a cibercriminales quienes prueban rutinas hasta acertar mediante pautas informadas. |
| **CT-04-011** | DELETE sin rol superior. Retorna → 403. | Ningun cambio adicional | - |
| **CT-04-012** | Respuesta OK incluye información base. Retorna → 200 OK. | Ningun cambio adicional |-|
| **CT-04-013** | Respuesta 404 retorna JSON estandarizado de error. Retorna → error 404 estándar. |Ningun cambio adicional | Si el sistema devuelve estructuras de error diferentes para un mismo fallo, la interfaz web (Frontend) se confundirá y no podrá mostrarlos correctamente al usuario. |
| **CT-04-014** | Suspender recurso en lazo vivo tipo pedido `PENDING`. Retorna → 200 OK. | Bloqueo a productos. Retorna → 200 OK e ignoramiento cascada, confirmando que transaccionados pedidos históricos se aislen y jamás corrompan órdenes previas pagadas. | Si un cliente ya pidió un producto y la orden está pendiente o pagada, desactivar el producto después no debería alterar ni borrar esa orden de la base de datos. |
| **CT-04-015** | Supresión genérica sobre totalidad base . Retorna → Arreglos nulos 200. |Supresión genérica sobre totalidad base . Retorna → Arreglos nulos 200 enviando al listado un arreglo netamente de corchetes vacíos obviando interrupciones por vacíos aparentes 404 . | Un menú vacío es un estado válido (por ejemplo, si se agotó todo para la venta). Enviar una lista vacía en lugar de un error evita que la página o app colapse repentinamente. |
| **CT-04-016** | Crear la orden referenciando anulados formalmente. Retorna → error. | Mandar orden con producto anulado . Retorna → 400 o 422 y aborta consolidar pago demostrando negatiemientos al inactivo id del alimento en la pasarela. | El sistema debe impedir que se pidan productos que ya no están disponibles en la carta.  |

---

## HDU-05 — Respuestas de Error JSON Estandarizadas (Desarrollador)

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-05-001** | Validación fallida: `tableId` fuera de rango. Retorna → 400. | Validación limitando de 0 a 13. Retorna → 400 emitiendo ErrorResponse que informa esclarecedoramente que solo existen de la 1 a la 12 como tope real válido . | Guiar hacia números permitidos suprime confusiones al instante por una simple vista garantizando reincorporaciones exitosas. |
| **CT-05-002** | Validación fallida: sin items. Retorna → 400. | Validación por omisiones al arreglo o valores purgados null. Retorna → 400 previniendo choques directos ante posibles NPE que tiran los ejecutables al vacío del sistema app. | Una comida en blanco no define ventas funcionales en sistemas restaurante, requerirlos fuertemente bloquea huecos inminentes de software java nativo sin caídas directas . |
| **CT-05-003** | Validación fallida: producto inexistente. Retorna → 400. | Validación de producto 999. Retorna → 400 detallando el ítem omitido en la DB logrando así avisarle al frontend sobre fallas al sicnronismo con la base principal en la vista de lista real. | Desfases constantes temporales en app propician requerimientos a elementos vacíos, comunicar ese ID agilizará correcitvas operacionales en piso directo por la cocina . |
| **CT-05-004** | JSON malformado. Retorna → 400. | JSON amputados o ilegibles al ojo nativo java . Retorna → 400 garantizando de modo perentorio envoltorio total al clásico Html nativo blando del servidor spring y encadenando un JSON robusto . | Exhibir fallas sucias al parsear devela códigos fuentes a escudriñadores expertos maliciosos forzando visiones de base técnica. |
| **CT-05-005** | GET /orders obviando header auth pre-designado. Retorna → 401. | GET sin token validante al header local maestro. Retorna → 401 Unauthorized adaptándolo forzosamente al diseño base de la organización por formato JSON a diferencia del defecto crudo HTML . | Desiguales respuestas ante apuros transaccionales quebran renderizados de UI quienes ignoran envoltorios HTML provocando avisos al infinito sin carga neta u origen útil explicatorio al cajero operante. |
| **CT-05-006** | GET /reports acarreando firma de token inválido. Retorna → 401. | GET bajo token nulo destrozado o falso. Retorna → 401 Unauthorized mimetizado igual sin dar avisos sobre si expiro tecnicmente o de hecho es completamente ilicito . | Enmascarar las bases del rechazo evita obsequios sobre información puntual la cual atacantes evalúan automatizadamente en prueba de intentos por acceso progresivo en el sitio de autenticidad web general . |
| **CT-05-007** | GET order apuntando UUID no localizable . Retorna → 404. | Búsqueda direccionando ID corrompido global o extraviado históricamente. Retorna → 404 Not Found asumiendo formalismo general de adjuntar factor identificador nativo `path` original al texto body. | Al estampar directamente ID extraviados al body logeable facilita revisiones a profundidad evitando retrasar tiempos de escrutinio log local correlacional. |
| **CT-05-008** | GET hacia items no existentes externamente. Retorna → 404. | Referenciar item que no reside base temporal global actual . Retorna → 404 Not Found replicando de base la estructura formal del error genérico original amoldándose para homologar reportes locales enteros . | Unificar estructura final a cualquier 404 indistintamente su ruta inicial simplifica rutinas receptoras UI centralizadamente en interfaces cliente web app. |
| **CT-05-009** | Report-service al caerse su conexión. Retorna → 500. | Corte directo con proveedor DB. Retorna → 500 cegando el error con caretas que esconden la dirección web, credencial privada o puertos enlazados de infraestructura interna . | Liberar variables físicas sobre topologías activas es regalar mapas vitales a piratería web , incurriendo falta grave según la norma general OWASP referida a entornos protegidos cerrados. |
| **CT-05-010** | Order-service experimentando Excepciones sin mapear global. Retorna → 500. | Cortar servicios al simular hilos caídos de java puros . Retorna → 500 atrapado a un asesor en `@ControllerAdvice` reestructurando el `text/html` no querido sobre Json nativo puro formal y cerrado . | Atajar nativamente excepciones ciegas impide que tomcats nativos o springs tiren pantallas en blanco al usuario limitando severas incomprensiones UX operativas para el comedor del día . |
| **CT-05-011** | Simetría estructural evaluable en sistema. Retorna → 400. | Inyectar igual fallo sobre 2 servicios (orden - reporte). Retorna → 400 evaluando asimetrías para dictaminar congruencias puras en formato, estructura exactita, tipados idénticos logrando simetría paralela. | Requerir paralelismo puro exime tiempo perdido en frontend programando parseos desiguales innecesarios, fortaleciendo flujos simples y directos ante los llamados api generales . |
| **CT-05-012** | Confirmar formato `content-type`. Retorna → application/json. | Interrogar fallos mixtos evaluando la procedencia cabecera constante. Retorna → headers de naturaleza `application/json` limitando al estricto parseo final. | Ignorancia del factor type condena al fracaso todo procesador externo de clientes web quienes basan sus integradores en el type JSON esperado truncando los fallbacks. |
| **CT-05-013** | Validaciones superpuestas de choque al tiempo. Retorna → 400. | Ejecutar llamadas sin mesa y datos puros `[]`. Retorna → 400 estableciendo normativas bases si evalua fail fast u ofrece la completitud formal dictaminada en contrato base resolutivo conjunto. | Formalizar normativas conjuntas disipa las resoluciones continuadas tediosas ofreciéndole al visualizador cliente opciones unidas conjuntas para que sane asimetrías sin recargas extra en api. |
| **CT-05-014** | Errores tolerantes a inyecciones. Retorna → Error estándar. | Infiltrar adjuntos extras al array del control `ErrorResponse`. Retorna → JSON estable evitando quiebres nativos asimilando a todo momento factores bases requeribles a toda vista mínima funcional externa base. | Aceptar adjuntos progresivos permite crecimientos maduros sobre reportes profundos en backend que no arruinan módulos débiles desactualizados al conservar compatibles base . |
| **CT-05-015** | Transición asíncrona ilógica estado vivo. Retorna → 409. | Enviar parche evaluativo estado preparativo directo final PENDING a READY. Retorna → 409 Conflict parando formalmente al avance garantista bloqueando transacciones cruzadas irrealizables a vista . | Romper máquina transicional fomenta incoherencias medibles sobre restaurantes; el freno impuesto garantiza fluidez oficial sin manipuleo o atajos perjudiciales operativos sobre cocina base final validante . |
