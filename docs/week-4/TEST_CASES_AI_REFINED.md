# Matriz de Ajuste


HDU-01 — Respuesta 201 Created con Header Location

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-01-001** | **Original:** Crear orden válida con 1 item. Valida tableId=5, productId=1, quantity=2, header Location presente. | **Ajustado:** Crear orden válida con 1 item. Valida tableId=5, productId=1, quantity=2, header Location presente con formato exacto: `http://localhost:8080/orders/{UUID}` siguiendo RFC 7231 (no `/orders/{id}` ambiguo). **Añadido:** Verificar que el UUID en Location es v4 (RFC 4122) y coincide con el campo `id` en el body de respuesta. **Añadido:** Validar que el header `Location` incluye protocolo (`http://` o `https://`) y host completo, no relativo. | **Negocio:** El header Location es esencial para REST estándar. Un header malformado causa que clientes REST genéricos no puedan navegar al recurso. **Técnica:** Asegurar que el formato es consistente en todos los navegadores/clientes HTTP. RFC 7231 exige URI absoluta, no relativa. El UUID v4 previene colisiones en escala distribuida. |
| **CT-01-002** | **Original:** Crear orden con múltiples items (3 ítems). Valida que respuesta contiene 3 items en array, cada uno con productId, quantity, note. | **Ajustado:** Crear orden con múltiples items (3 ítems). Valida que respuesta contiene exactamente 3 items en array, **en el mismo orden que se enviaron**. Cada item debe tener productId, quantity, note, y **además `pricePerUnit` y `subtotal`** derivados del catálogo. **Añadido:** Verificar que el JSON retornado es válido y que `items[0].pricePerUnit` coincide con el precio en el catálogo para productId=1. **Añadido:** Validar que `order.totalPrice = sum(items[i].subtotal)`. | **Negocio:** El orden de los ítems importa para UI (muestra en ticket POS en el mismo orden). Los precios deben venir del catálogo en ese momento (no cambiar retroactivamente si se modifica el menú). El cálculo de totales debe ser auditable. **Técnica:** JSON debe serializar items en orden determinista. |
| **CT-01-003** | **Original:** Crear orden en tableId=12 (límite superior). Valida 201 Created, evento publicado, tableId persiste. | **Ajustado:** Crear orden en tableId=12 (límite superior). Valida 201 Created, evento publicado, tableId persiste. **Añadido:** Verificar que se rechaza tableId=13 (justo encima del límite) con 400/422. **Añadido:** Validar que en concurrent load (múltiples órdenes simultáneas en tableId=12) todas se crean exitosamente sin race conditions. **Añadido:** Verificar que la BD tiene índice en (tableId, status) para queries eficientes. | **Negocio:** Las mesas en un restaurante son recursos finitos (número físico de mesas). Boundary value analysis requiere probar 12 y 13 juntos para confirmar límite exacto. Load concurrente simula servicio al medio día. **Técnica:** Índices en BD evitan table scans que ralentizan queries. |
| **CT-01-004** | **Original:** Crear orden en tableId=1 (límite inferior). Valida 201 Created, persiste correctamente. | **Ajustado:** Crear orden en tableId=1 (límite inferior). Valida 201 Created, persiste correctamente. **Añadido:** Verificar que tableId=0 es rechazado con 400/422. **Añadido:** Validar que en múltiples órdenes simultáneas en tableId=1 (mesa con mala ubicación pero válida), no hay deadlocks ni pérdida de órdenes. **Añadido:** Verificar que logs muestren audit trail: quién creó, cuándo, desde qué IP. | **Negocio:** Aunque tableId=1 es límite válido, es importante que el sistema maneje correctamente mesas de alto tráfico (ej: entrada, más interrupciones). Auditoría de creación es crítica para resolver disputes (cliente dice "nunca pedí eso"). **Técnica:** Logs estructurados (JSON logs) facilitan búsqueda post-mortem en Elasticsearch/CloudWatch. |
| **CT-01-005** | **Original:** Orden con nota en item. Item con nota="Sin lácteos". Valida que nota se retorna exactamente igual en respuesta y RabbitMQ event. | **Ajustado:** Orden con nota en item. Item con nota="Sin lácteos". Valida que nota se retorna exactamente igual en respuesta y RabbitMQ event. **Añadido:** Validar que caracteres especiales en nota (ej: "Sin <lácteos & gluten>") se escapan correctamente en JSON (no se corrompe). **Añadido:** Probar que notas largas (>500 caracteres) se aceptan pero se truncan a 500 en BD (con warning). **Añadido:** Verificar que la nota se visibiliza correctamente en cocina (pantalla de orden). | **Negocio:** Las notas especiales (alergias, preferencias) son CRÍTICAS en un restaurante. Un error de escaping causa que alergias no se transmitan. Truncamiento previene inyección de datos. **Técnica:** JSON escaping (RFC 7159) es obligatorio. Truncamiento es estrategia de defensa contra ataques DoS. |
| **CT-01-006** | **Original:** tableId=13 retorna 400/422, no publica evento, no persiste orden. | **Ajustado:** tableId=13 retorna 400/422 (se distingue correctamente entre ambos códigos). **Añadido:** Validar que NO se publica evento en RabbitMQ. **Añadido:** Validar que la BD no tiene registro de intento (no contamina logs). **Añadido:** Verificar que error message es "tableId must be between 1 and 12", no expone detalles técnicos. **Añadido:** Probar que response time es < 100ms (validación rápida, no acceso a BD). | **Negocio:** Rechazos rápidos evitan que colas de atención se saturen procesando requests inválidos. Errores claros permiten que UI educque al usuario ("Mesa inválida, revisa"). **Técnica:** Validación early en controlador (no en servicio/BD). |
| **CT-01-007** | **Original:** tableId=0 retorna 400/422, orden no se crea. | **Ajustado:** tableId=0 retorna 422 Unprocessable Entity (es un valor semánticamente inválido, no formato incorrecto). **Añadido:** Mensaje de error: "Mesa inválida: tableId debe estar entre 1 y 12". **Añadido:** Probar que tableId="" (string vacío) retorna 400 Bad Request (tipo incorrecto). **Añadido:** Verificar que tableId=NaN retorna 400 Bad Request. | **Negocio:** Distinguir 400 vs 422 es crítico para que frontend distinga "usuario escribió mal" de "número inválido". **Técnica:** Jackson rechaza 400, reglas de negocio rechazan 422. |
| **CT-01-008** | **Original:** tableId=-5 retorna 400/422. | **Ajustado:** tableId=-5 retorna 422 Unprocessable Entity (es un entero válido, pero semánticamente inválido). **Añadido:** Mensaje: "tableId must be > 0". **Añadido:** Probar secuencia: -1, 0, 1 para confirmar límite exacto. **Añadido:** Validar que en logs no aparece tableId=-5 (sensibilidad a datos negativos). | **Negocio:** Números negativos son errores comunes de lógica (inversión de signo). Validación firme evita corrupciones silenciosas. **Técnica:** Validadores Spring (@Min, @Max) detectan esto automáticamente. |
| **CT-01-009** | **Original:** items=[] retorna 400/422, error menciona "al menos 1 item", no publica evento, no persiste. | **Ajustado:** items=[] retorna 422 Unprocessable Entity (JSON válido, pero regla de negocio viola "al menos 1 item"). **Añadido:** Mensaje: "Order must contain at least one item. Please select products from menu." **Añadido:** Probar items=null retorna 400 Bad Request (null != array vacío). **Añadido:** Verificar que respuesta time es < 200ms (puede involucrar validación de items). **Añadido:** Validar que en logs se registra intento fallido (para análisis de patrones de uso / UX issues). | **Negocio:** Órdenes vacías son logísticamente imposibles. Error claro ayuda a UX (no deja que user vea "enviar" si no hay items). Logs de intentos fallidos revelan UX problems (ej: "muchos users intentan crear órdenes vacías", indica que botón de "agregar" falla). **Técnica:** 422 es más preciso que 400. Registro de fallos facilita análisis post-mortem. |
| **CT-01-010** | **Original:** productId=999 no existe. Retorna 400/422, especifica "producto no existe", no persiste. | **Ajustado:** productId=999 no existe. Retorna 404 Not Found (recurso "producto" no existe). **Consideración:** Alternativamente, 422 Unprocessable Entity si se considera "orden no puede procesarse sin ese producto". **Añadido:** Validar que error message es "Product 999 not found in menu" (no expone ID de BD interno si es diferente). **Añadido:** Probar race condition: productId=1 existe, se crea orden, luego se desactiva el producto en paralelo. Verificar que orden ya creada NO es rechazada retroactivamente. | **Negocio:** Productos no existen es un error de referencia, no de validación. El sistema debe manejar desactivaciones de menú sin afectar órdenes ya creadas. **Técnica:** Foreign key constraint en BD (si está en restaurante_db). Test de concurrencia revela race conditions. |
| **CT-01-011** | **Original:** productId=2 con is_active=false retorna 400/422, error menciona "no disponible", no persiste. | **Ajustado:** productId=2 con is_active=false retorna 422 Unprocessable Entity (producto existe, pero no es válido para crear orden). **Añadido:** Mensaje: "Product 2 (Caldo) is currently unavailable (deactivated by kitchen manager at 2026-03-04 09:30)". **Nota:** Este nivel de detalle requiere UNION con tabla de auditoría, considerar costo-beneficio. **Simplificado a:** "Product 2 is not available for orders". **Añadido:** Probar que si se desactiva un producto DURANTE la creación de la orden (race condition), la orden se rechaza (rollback de transacción). | **Negocio:** Productos inactivos son temporalmente fuera de stock (ej: sin ingredientes). El sistema debe impedir órdenes con productos desactivados. Manejo de race conditions evita órdenes fantasma. **Técnica:** @Transactional en servicio, snapshot isolation en BD. |
| **CT-01-012** | **Original:** quantity=0 retorna 400/422, error menciona "cantidad debe ser positiva". | **Ajustado:** quantity=0 retorna 422 Unprocessable Entity (es un número, pero semánticamente inválido). **Añadido:** Mensaje: "Quantity must be >= 1. You cannot order 0 items." **Añadido:** Validar que quantity=0.5 (float en lugar de int) retorna 400 Bad Request (tipo incorrecto). **Añadido:** Probar que quantity=1 (límite mínimo válido) es aceptado. **Añadido:** Verificar comportamiento en UI: ¿permite reducir quantity a 0 o tiene "Remove item" separado? (Alineación UI-API). | **Negocio:** quantity=0 es una no-operación. UI debe ofrecer "Remove item" en lugar de "set quantity to 0". Esta prueba revela posibles desalineaciones API-Frontend. **Técnica:** @Min(1) en DTO valida automáticamente. |
| **CT-01-013** | **Original:** quantity=-3 retorna 400/422. | **Ajustado:** quantity=-3 retorna 422 Unprocessable Entity. **Añadido:** Mensaje: "Quantity cannot be negative". **Añadido:** Boundary test: probar quantity=-1, -0 (aunque raro). **Añadido:** Validar que quantity tiene límite superior sensato (ej: max 999 por item, para evitar DoS de cantidad). Prueba: quantity=1000000 → ¿rechaza con 422, o acepta causando problemas downstream? | **Negocio:** Cantidades negativas son aberraciones. Límites superiores previenen ataques de cantidad masiva (ej: intentar pedir 1M unidades para sobrecargar cocina). **Técnica:** @Max(999) adicional. Validar en tests si existe. |
| **CT-01-014** | **Original:** tableId="mesa5" (string) retorna 400 Bad Request, error indica "tipo de dato incorrecto". | **Ajustado:** tableId="mesa5" (string) retorna 400 Bad Request (correcto, JSON deserialization falla). **Añadido:** Validar que error message es "Invalid type for field tableId: expected integer, got string" (no expone stack trace). **Añadido:** Probar tableId=5.0 (float válido en JSON, pero campo espera int) - comportamiento depende de Jackson config (puede aceptar o rechazar). **Recomendación:** Rechazar floats (422 o 400 según política). **Añadido:** Verificar que respuesta time es < 100ms (validación de tipo es rápida, sin BD). | **Negocio:** JSON type validation es primera línea de defensa. Errores claros orientan developers. **Técnica:** Jackson settings `DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES = false` (ignorar campos extra) pero sí fallar en tipos incorrrectos. |
| **CT-01-015** | **Original:** JSON malformado (llave sin cerrar) retorna 400 Bad Request, error describe problema de parseo. | **Ajustado:** JSON malformado (llave sin cerrar) retorna 400 Bad Request (correcto). **Añadido:** Validar que mensaje de error NO contiene línea/columna del error (información de debug, no para producción), pero sí mensaje general "Invalid JSON format". **Añadido:** Probar varios malformatos: `{"tableId": 5` (sin cerrar), `{"tableId": 5,}` (trailing comma), `{tableId: 5}` (sin comillas en key). **Añadido:** Verificar que parser falla rápidamente (< 50ms) incluso en JSON muy grande (defensa contra Billion Laughs / billion-node attack). | **Negocio:** JSON parsing errors son comunes. Rechazo rápido evita que Request malformado agote recursos. **Técnica:** Jackson default settings son seguros (rechaza comentarios, NaN, etc.). |
| **CT-01-016** | **Original:** quantity=999999 (muy grande). Retorna 201 Created, persiste con esa cantidad. Nota: "Sin validación de límite superior explícita". | **Ajustado:** quantity=999999 (muy grande) → **DEBE retornar 422 Unprocessable Entity** (no 201). **Razón:** Un restaurante no puede preparar 999K unidades de un item. **Recomendación:** Implementar `@Max(500)` en validador o regla de negocio "cantidad máxima por item = 50" (típico en QSR/fast-casual). **Alternativa:** Aceptar 999999 pero registrar warning en logs. **Prueba:** quantity=501 → validar rechazo con 422. | **Negocio:** Límites superiores previenen: 1) errores de input humano (usuario presiona "9" múltiples veces), 2) ataques DoS (intentar crear orden de 999M items para causar Out Of Memory), 3) errores de lógica (inversión accidental de cantidades). **Técnica:** @Max, @Range, o validador personalizado. Sin este, riesgo de seguridad media. |
| **CT-01-017** | **Original:** Múltiples items del mismo producto (productId=1 qty=2 y qty=3). Retorna 201, ambos items persisten por separado (no consolidados). | **Ajustado:** Múltiples items del mismo producto (productId=1 qty=2 y qty=3). Retorna 201, ambos items persisten por separado (correcto, no consolidar). **Añadido:** Validar que respuesta contiene exactamente 2 items en array (no consolidado a qty=5). **Razón:** Consolidación ocultaría información de notas/preferencias diferentes (ej: una porción sin sal, otra normal). **Añadido:** Prueba de UI: ¿cómo muestra UI 2 items del mismo producto? (Usabilidad). **Añadido:** Verificar que evento RabbitMQ incluye ambos items: `items[{productId:1,qty:2},{productId:1,qty:3}]`, no consolidado. | **Negocio:** En restaurante, un cliente puede pedir 2 cafés de diferente forma (uno con leche, otro puro). No consolidar preserva esta información. Cocina ve: "2x Café" en UI, pero con anotaciones diferentes. **Técnica:** items es array ordenado, no Set o Map. Validar en respuesta y evento. |
| **CT-01-018** | **Original:** Nota vacía (""). Retorna 201, nota se guarda como null o "". | **Ajustado:** Nota vacía (""). Retorna 201 (correcto, nota es opcional). **Comportamiento:** Nota se guarda como null en BD (no como string vacío), para distinguir "no hay nota" de "nota es string vacío". **Añadido:** Validar que GET /orders/{id} retorna note: null (no note: ""). **Consideración:** Si DB schema usa NOT NULL, cambiar a NULL DEFAULT. **Alternativa:** Guardar como "" y en API normalizar a null. **Prueba:** Validar que GET devuelve note:null consistentemente. | **Negocio:** null vs "" es distinción semántica importante (NULL significa "no especificado", "" significa "especificado pero vacío"). Consistencia en API reduce bugs en frontend. **Técnica:** JPA @Column(nullable=true), Jackson serializa como null. |
| **CT-01-019** | **Original:** Nota con caracteres especiales ("¡Sin sal, pimentón! & <>"). Retorna 201, nota se persiste exactamente igual (escapada si necesario). | **Ajustado:** Nota con caracteres especiales ("¡Sin sal, pimentón! & <>"). Retorna 201, nota se persiste y retorna exactamente igual (JSON escaping automático vía Jackson). **Importante:** Validar que en BD se almacena literal (sin escaping), pero API la retorna con escaping JSON. **Ejemplo:** DB: `¡Sin sal!`, API responde: `\"¡Sin sal!\"` (escaping transparente a usuario). **Añadido:** Probar UTF-8 characters (ñ, é, 中文) para confirmar encoding global. **Añadido:** Prueba de injection: nota="`<script>alert('xss')</script>`" → validar que se persiste como string literal, no ejecuta script (defensa contra XSS vía notas). | **Negocio:** Caracteres especiales y UTF-8 son comunes (español tiene ñ, restaurante puede servir culinaria mundial). XSS prevention es crítica (nota maliciosa no debe ejecutar en navegador de cocina). **Técnica:** Jackson escaping es automático. Validar que no hay HTML rendering en UI sin sanitización. |
| **CT-01-020** | **Original:** Header Location contiene UUID v4 válido en formato "http://localhost:8080/orders/{UUID-v4}". | **Ajustado:** Header Location contiene UUID v4 válido en formato "http://localhost:8080/orders/{UUID-v4}" (o "https://..." en producción). **Añadido:** Validar que UUID es RFC 4122 v4 (no v1 o custom). **Validación:** UUIDs v4 tienen patrón `xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx` donde y ∈ [8,9,a,b]. **Añadido:** Probar que dos órdenes creadas consecutivas tienen UUIDs diferentes (aleatorios, no secuenciales). **Razón:** UUIDs secuenciales revelan patrón, reducen entropía. **Prueba:** Crear 10 órdenes, verificar todos tienen UUIDs diferentes y válidos. **Edge case:** ¿Qué si UUID collision ocurre (extremadamente raro)? Sistema debe reintentarlo. | **Negocio:** UUIDs permiten referencias globales sin exponer secuencias internas (mecanismo de privacidad: no se ve que se crearon 1000 órdenes si IDs no son 1,2,3...). **Técnica:** UUID v4 (random) vs v1 (timestamp-based) - v4 es mejor para privacidad. Java: `UUID.randomUUID()` es seguro. |
| **CT-01-021** | **Original:** Evento order.placed contiene eventVersion=1 exactamente (no "1", no null, no 2). Estructura: eventVersion, eventType, orderId, tableId, items, timestamp. | **Ajustado:** Evento order.placed contiene eventVersion=1 exactamente (número, no string). **Validación:** Si eventVersion != 1, se rechaza (envío a DLQ per specs). **Estructura:** Validar que evento es: `{eventVersion: 1, eventType: "order.placed", orderId: UUID, tableId: 1-12, items: [...], timestamp: ISO8601, ...}`. **Añadido:** Verificar que orderCreatedAt en evento coincide exactamente con createdAt en API response (mismo timestamp, no diferencias de ms). **Añadido:** Probar que evento es publicado ANTES de que respuesta 201 se envíe al cliente (garantía de durabilidad). **Técnica:** Use `@Transactional(Propagation.REQUIRES_NEW)` para publicar evento en transacción separada, garantizando que si falla, cliente no ve 201. | **Negocio:** eventVersion es critical para schema evolution (si sistema cambia formato a v2, kitchen-worker v1 sabe rechazarlo). Timestamp exact match previene inconsistencias. Durabilidad garantiza que orden no se "pierde" si client desconecta. **Técnica:** RabbitMQ persistence, Spring @TransactionalEventListener. |
| **CT-01-022** | **Original:** Orden duplicada (mismo payload enviado dos veces). Ambas retornan 201, con UUIDs diferentes. Dos órdenes en DB. Nota: "Sistema no implementa idempotencia de cliente". | **Ajustado:** Orden duplicada (mismo payload enviado dos veces en < 1 segundo). Ambas retornan 201, con UUIDs diferentes (correcto, sin cliente-level idempotency token). **Importante:** Este es un comportamiento válido para MVP (sin deduplicación), pero requiere documentación. **Recomendación para V2:** Implementar cliente idempotency token (header `Idempotency-Key: {UUID}` para deduplicar retries). **Prueba actual:** Validar que sin Idempotency-Key, dos requests = dos órdenes (comportamiento esperado). **Prueba de resiliencia:** Enviar mismo payload 10 veces rápidamente (simular retry loop), validar que se crean 10 órdenes (no crash, no deadlock). | **Negocio:** Sin idempotencia, clientes debe ser cuidadosos (no presionar "submit" dos veces). Riesgo de duplicados en caso de network retry. MVP acceptable si documentado, pero V2 debe implementar. **Técnica:** Idempotency-Key + cache (Redis) para deduplicación. |
| **CT-01-023** | **Original:** Orden creada, se consulta GET /orders/{id} inmediatamente. Evento ya existe en RabbitMQ topic (o está en vías de estarlo). Orden existe en restaurant_db. | **Ajustado:** Orden creada exitosamente (201), se consulta GET /orders/{id} inmediatamente (< 100ms). **Expected:** Orden existe en respuesta GET (lectura desde restaurant_db, que está en sync con POST). **Consideración:** Si hay replicación/sharding de BD, puede haber lag. **Para test:** Usar single-node BD setup (desarrollo/testing). **Validar:** GET retorna exactamente mismos datos que POST response body (id, tableId, items, status=PENDING, createdAt, updatedAt). **Evento:** RabbitMQ event puede estar en flight o ya procesado (timing no garantizado), pero debe existir cuando se escucha el topic. No es crítico que exista antes de GET (diferentes sistemas). | **Negocio:** Consistencia Read After Write es fundamental (usuario ve que creó la orden inmediatamente). Sin esto, confusión ("¿se guardó mi orden?"). **Técnica:** Single-writer principle en restaurant_db (solo order-service escribe orders). Reads pueden ser from replicas (eventual consistency acceptable con lag < 1s). |
| **CT-01-024** | **Original:** Respuesta 201, header Content-Type es "application/json". Respuesta es JSON válido. | **Ajustado:** Respuesta 201, header Content-Type es "application/json; charset=UTF-8" (explícitamente UTF-8). **Validación:** Parsear response body como JSON (no plain text). **Añadido:** Verificar que response no contiene BOM (Byte Order Mark), que algunos parsers rechazan. **Importante:** Charset debe ser UTF-8 para soportar caracteres especiales (ñ, acentos, caracteres asiáticos). **Test:** Enviar orden con nota="Café con leche", validar que respuesta es parseable como JSON con caracteres preservados. | **Negocio:** charset=UTF-8 es estándar internacional, permite restaurantes de cualquier región. Sin especificar, clientes pueden suponer ISO-8859-1, causando garbling. **Técnica:** Spring default es UTF-8, pero explicitarlo en header es buena práctica. |
| **CT-01-025** | **Original:** Respuesta 201 contiene: id (uuid), tableId (number), status (PENDING), items (array), createdAt (ISO 8601), updatedAt (ISO 8601). | **Ajustado:** Respuesta 201 contiene campos obligatorios: `id` (UUID string), `tableId` (integer, 1-12), `status` (string, exactly "PENDING"), `items` (array de {productId, quantity, note, pricePerUnit, subtotal}), `createdAt` (ISO 8601 timestamp), `updatedAt` (ISO 8601 timestamp, == createdAt). **Añadido:** Validar que `id` es no-null y matches UUID en Location header. **Añadido:** Validar que createdAt == updatedAt (orden nueva, no modificada). **Timestamp format:** ISO 8601 con Z suffix (UTC), ej: "2026-03-04T10:30:45.123Z". **Validar:** Parseable como instant en JS, Python, Java. **Importante:** No incluir otros campos (ej: deletedAt, deletedBy, no deben estar en respuesta si no son necesarios). | **Negocio:** Consistencia de contrato es crítica. Frontend depende de estos campos. Campos sorpresa causan bugs. **Técnica:** Jackson DTO con `@JsonInclude(NON_NULL)` para no serializar nulos, `@JsonProperty(required=true)` para marcar obligatorios. |
| **CT-01-026** | **Original:** GET a recurso creado sin token retorna 200 OK, cuerpo contiene orden. | **Ajustado:** GET a recurso creado sin token (no env envía X-Kitchen-Token) retorna 200 OK (correcto, endpoint público para consultarorden). **Importante:** Confirmar que GET /orders/{id} NO requiere autenticación (MVP scope: sin auth en lectura de orden individual). **Consideración:** Para MVP es acceptable, pero V2 puede requerir token para acceso a órdenes de clientes específicos. **Prueba:** GET /orders/{id} sin token = 200 OK (público), GET /orders?status=PENDING sin token = ¿401? (chef list puede requerir token). **Revisar:** Spec si GET /orders (listado) vs GET /orders/{id} (individual) tienen reglas diferentes. | **Negocio:** Acceso público a orden individual permite cliente verificar estado sin login (útil para restaurantes sin auth). Listado sin token es riesgo (expone todas órdenes). **Técnica:** Configurar seguridad por endpoint: `/orders/{id}` público, `/orders` protegido. |
| **CT-01-027** | **Original:** GET a recurso con X-Kitchen-Token: cocina123 retorna 200 OK. Orden completa con status PENDING. | **Ajustado:** GET a recurso con X-Kitchen-Token válido retorna 200 OK. Orden completa con status PENDING. **Importante:** Validar que token es validado (no acepta cualquier string). **Test:** X-Kitchen-Token: "invalid" retorna ¿401 Unauthorized? o 200 OK (pues GET /orders/{id} es público). **Clarificar:** ¿GET /orders/{id} requiere token o no? Asumir MVP = no requiere (público). **Prueba:** GET con token=cocina123 (válido) = 200 OK, GET sin token = 200 OK (ambos devuelven la orden). **Diferencia esperada:** Si orden tuviera campo `sensitive` (ej: cliente payment), token determinaŕía visibilidad. Para MVP, no aplica. | **Negocio:** GET /orders/{id} público permite chef acceder sin auth (si cocina está ocupada, sin tiempo de loguearse). Trade-off: privacidad vs conveniencia. **Consideración:** UUIDs son suficientemente aleatorios que adivinar es computacionalmente infeasible, mitigando riesgo de acceso no autorizado. |
| **CT-01-028** | **Original:** Orden recién creada, GET /orders/{id} retorna status="PENDING" exactamente. No puede haber sido modificado por otro consumer. | **Ajustado:** Orden recién creada, GET /orders/{id} retorna status="PENDING" exactamente (string, no enum, no null). **Garantía:** status NO puede cambiar entre POST (que retorna 201 con status=PENDING) y GET inmediato (< 100ms). **Razón:** Protección contra race condition where kitchen-worker procesa evento y cambia status a IN_PREPARATION antes de que cliente lea. **Implementación:** POST retorna snapshot de orden en ese momento, no referencia viva. GET lee estado actual. Diferencia esperada < 100ms. **Prueba de concurrencia:** Crear orden, inmediatamente iniciar listener de RabbitMQ (simular kitchen-worker), luego GET el orden. Validar que GET refleja cambio si listener es rápido. | **Negocio:** Cliente ve orden creada en PENDING. Si cocina empieza inmediatamente, cliente puede ver transición a IN_PREPARATION. Esto es correcto. Probar que transición sucede sin errores. **Técnica:** Optimistic locking (version field) previene lost updates si múltiples servicios modifican. |

---

## Matriz de Ajuste: HDU-02 — Respuesta 200 OK con Lista Vacía

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-02-001** | **Original:** GET /menu con DB vacía o todos productos inactivos. Retorna 200 OK, Content-Type JSON, body es exactamente []. NO es null. | **Ajustado:** GET /menu con DB vacía o todos productos inactivos. Retorna 200 OK, Content-Type JSON, body es exactamente [] (empty JSON array). **Validación:** Content-Type es "application/json; charset=UTF-8". **Importante:** Validar que "exactamente" significa: (a) no espacios extras ([] no [ ]), (b) no null, (c) no undefined. **Prueba de parser:** Parsear JSON response, validar que es array con length=0. **Negocio edge case:** Si se desactivan todos los productos (ej: restaurante cierra temporalmente), GET /menu retorna vacío (correcto, no hay nada para pedir). | **Negocio:** Menú vacío es un estado válido (restaurante cerrado, sin menú). API debe retornar 200 (no 503 Service Unavailable), permitiendo que cliente muestre "Menú no disponible" gracefully. **Técnica:** DTOs inicializan `items = new ArrayList<>()` (no null). Jackson serializa [] no null. |
| **CT-02-002** | **Original:** GET /menu con 3 productos activos. Retorna 200 OK, array con exactamente 3 elementos. Cada contiene: id, name, description, price, category, imageUrl, isActive. | **Ajustado:** GET /menu con 3 productos activos. Retorna 200 OK, array con exactamente 3 elementos en orden determinista (ej: por id ASC o nombre ASC). **Validación:** Cada producto contiene: `id` (integer), `name` (string, no-null), `description` (string), `price` (decimal, >= 0), `category` (string), `imageUrl` (string or null), `isActive` (boolean, must be true). **Añadido:** Validar que productos inactivos NO aparecen (filtro en BD query). **Considera:** ¿Incluir campos adicionales (ej: stock, disponibilidad)? Para MVP, excluir para mantener contrato simple. **Prueba:** GET /menu múltiples veces, validar que orden es siempre igual (determinismo para caché). | **Negocio:** Menú es referencia del catálogo. Orden determinista facilita client-side caching (si GET 1 y GET 2 son idénticos, puede usar cached). **Técnica:** Sort by id DESC o name ASC en SQL query (@Query con ORDER BY). |
| **CT-02-003** | **Original:** 5 productos: 3 activos (is_active=true), 2 inactivos (is_active=false). GET /menu retorna 200, array con exactamente 3 (solo activos). | **Ajustado:** 5 productos en BD: 3 con is_active=true, 2 con is_active=false. GET /menu retorna 200 OK, array con exactamente 3 elementos (solo activos). **Validación:** Confirmar que filter es `is_active = true` en query (no post-processing). **Performance:** Para BD grande (1000 productos), índice en (is_active, id) acelera query. **Edge case:** Si todos están inactivos (is_active=false), GET /menu retorna [] (no error). **Prueba:** Desactivar todos 3 activos, GET /menu → validar []. Reactivar 1, GET /menu → validar array con 1 elemento. | **Negocio:** Desactivación de productos (stock agotado, temporada cerrada) es operación frecuente. Filtrado correcto asegura que clientes no vean productos no disponibles. **Técnica:** Spring Data JPA @Query("SELECT p FROM Product p WHERE p.isActive = true") o @Query(value="SELECT * FROM menu_items WHERE is_active=1"). |
| **CT-02-004** | **Original:** GET /reports sin órdenes READY en rango 2026-01-01 a 2026-01-31. Retorna 200 OK, body contiene `{totalRevenue: 0.0, orders: [], productSummary: []}`. | **Ajustado:** GET /reports?startDate=2026-01-01&endDate=2026-01-31 sin órdenes READY en ese rango. Retorna 200 OK, Content-Type JSON. Body contiene: `{totalRevenue: 0.0, orders: [], productSummary: []}`. **Validación:** totalRevenue es número (0.0 or 0, no string "0"). **Añadido:** Validar que orders y productSummary son arrays, ambos vacíos (no null, no omitidos). **Endpoint security:** GET /reports requiere X-Kitchen-Token (si SPEC exige). **Prueba:** Sin token → ¿401 Unauthorized? Asumir que GET /reports es protegido (diferente de GET /menu público). | **Negocio:** Reportes sin datos son válidos (restaurante nuevo, sin ventas). Retornar 200 OK (no 204 No Content) con estructura consistente permite frontend parsear siempre misma estructura. **Técnica:** ReportResponseDTO inicializa arrays. |
| **CT-02-005** | **Original:** GET /reports sin órdenes READY. totalRevenue es exactamente 0 o 0.0, productSummary es array vacío []. | **Ajustado:** GET /reports sin órdenes READY en cualquier rango. totalRevenue es exactamente 0 o 0.0 (number, no string, no null). **Consideración:** ¿0 o 0.0? Para reportes, usar decimal (0.0) para consistencia con otros valores monetarios que pueden ser decimales (ej: 123.45). **Validación:** Number.isFinite(totalRevenue) && totalRevenue >= 0 (no NaN, no Infinity). **productSummary:** Array vacío [], nunca null o undefined. **Edge case:** Si una orden READY tiene precio 0 (gratis), totalRevenue sigue siendo 0. Asegurar que no incluye órdenes PENDING/IN_PREPARATION (solo READY). | **Negocio:** Reportes monetarios requieren precisión decimal. totalRevenue es agregación que debe ser auditabel. **Técnica:** SQL SUM(price) retorna null si no hay rows, code debe convertir a 0.0. |
| **CT-02-006** | **Original:** GET /reports para rango futuro (2099-01-01 a 2099-12-31). Retorna 200 OK, conteos/sumatorias son 0. | **Ajustado:** GET /reports?startDate=2099-01-01&endDate=2099-12-31 (futuro). Retorna 200 OK, estructura es: `{totalRevenue: 0.0, orders: [], productSummary: []}` (no error, aunque no hay datos). **Validación:** Parámetros de fecha son aceptados sin error (validación es lógica: startDate < endDate, ambos en rango válido). **Boundary:** Probar startDate > endDate (ej: 2099-12-31 a 2099-01-01) → ¿400 Bad Request o 422 Unprocessable Entity? **Futura:** Si hay datos históricos (ej: año 2099 tiene órdenes preloaded para testing), reports los incluye. Para test, asumir BD vacía. | **Negocio:** Reportes futuros son válidos (previsión, archiving). Retornar estructura vacía es correcto. Validación de rango (start <= end) evita queries sin sentido. **Técnica:** @DateTimeFormat en parámetros, @Positive o @Future validadores. |
| **CT-02-007** | **Original:** GET /orders sin parámetros, tabla vacía o todos deleted=true. Retorna 200 OK, body es exactamente []. | **Ajustado:** GET /orders sin parámetros (no query params), tabla restaurant_db.orders vacía o todos tienen deleted=true. Retorna 200 OK, body es exactamente []. **Validación:** Soft-deleted orders (deleted=true) NO aparecen en listado (query filtra deleted=false or null). **Edge case:** Si algunos deleted=true y algunos deleted=false, listado incluye solo not-deleted. **Parámetros:** GET /orders sin ?page, ?size, ?status → default behavior (asumir: retorna todas, no paginadas, o con límite por defecto). Confirmar en SPEC. | **Negocio:** Soft-deleted órdenes son eliminadas lógicamente (no visibles), pero preservadas en BD para auditoría. GET /orders excluye automáticamente. **Técnica:** JPA @Where(clause="deleted=false") en entity, o filtro manual en repository query. |
| **CT-02-008** | **Original:** GET /orders?status=PENDING sin órdenes PENDING. Retorna 200 OK, array "orders" es vacío []. NO incluye otras órdenes. | **Ajustado:** GET /orders?status=PENDING sin órdenes con status=PENDING. Retorna 200 OK, body es exactamente []. **Validación:** Filtro funciona correctamente (no retorna órdenes IN_PREPARATION o READY). **Edge case:** GET /orders?status=INVALID_STATUS → ¿400 Bad Request o 422? Asumir validación de enum (status debe ser PENDING, IN_PREPARATION, o READY). **Query:** GET /orders?status=PENDING,IN_PREPARATION (múltiples) → Si supported, retorna solo esos dos estados. Clarificar en spec. | **Negocio:** Filtros correctos permiten cocina ver "todas las órdenes PENDING" (lo que necesita cocinar ahora). **Técnica:** @RequestParam(required=false) OrderStatus status, en query: `WHERE status IN (:status)` con default si null. |
| **CT-02-009** | **Original:** GET /menu sin token retorna 200 OK (público, no requiere X-Kitchen-Token). | **Ajustado:** GET /menu sin X-Kitchen-Token header retorna 200 OK (confirmado, endpoint público). **Validación:** Mismo resultado con o sin token (token ignorado si enviado). **Razón:** Clientes acceden a menú sin autenticación (MVP scope). **Consideración:** Si futuro requiere autenticación en GET /menu (ej: acceso de miembros loyalty), cambiar a 401 sin token. **Prueba actual:** GET /menu sin token = 200 OK, GET /menu con válido token = 200 OK (ambos idénticos). | **Negocio:** Menú público (sin login) es más accesible. Catering, delivery apps, anyone puede consultar menú. **Seguridad:** No hay datos sensibles en menú (public info). |
| **CT-02-010** | **Original:** GET /reports sin X-Kitchen-Token retorna 401 Unauthorized (no lista, sino error JSON). Mensaje menciona autenticación requerida. | **Ajustado:** GET /reports?startDate=...&endDate=... sin X-Kitchen-Token header retorna 401 Unauthorized. **Importante:** Respuesta es JSON estandarizado de error, no una lista vacía. **Estructura:** `{timestamp: ISO, status: 401, error: "Unauthorized", path: "/reports", message: "X-Kitchen-Token header is missing or invalid"}`. **Validación:** NO retorna [] o 200 OK (protegido, diferente de /menu). **Razón:** Reportes exponen información sensible (revenue, sales), acceso restringido a cocina/admin. | **Negocio:** Reportes de revenue/sales son confidenciales. Requieren autenticación. Diferencia clara 200 OK (menú) vs 401 (reportes) es intencional. **Técnica:** Spring Security @PreAuthorize or @Secured en reportes. |
| **CT-02-011** | **Original:** GET /reports con X-Kitchen-Token: "invalid_token_xyz". Retorna 401 Unauthorized. | **Ajustado:** GET /reports?startDate=...&endDate=... con X-Kitchen-Token: "invalid_xyz". Retorna 401 Unauthorized. **Validación:** No se procesa la query, se rechaza early por invalid token. **Token validation:** Asumir que sistema valida token (ej: contra hardcoded "cocina123" en MVP, o Redis cache en V2). **Prueba:** Valid token (ej: "cocina123") → 200 OK, invalid token → 401 Unauthorized. **Mensaje de error:** No expone detalles de por qué es inválido (no "token signature mismatch", genérico es mejor). | **Negocio:** Tokens inválidos son rechazados explícitamente. Protección básica de acceso. **Técnica:** Token validation en HandlerInterceptor o @Component filter. |
| **CT-02-012** | **Original:** GET /orders?status=PENDING sin X-Kitchen-Token. Retorna 401 Unauthorized (no lista). | **Ajustado:** GET /orders?status=PENDING sin X-Kitchen-Token header. Retorna 401 Unauthorized. **Importante:** GET /orders (listado con filtro de estado) es PROTEGIDO (requiere token). Diferente de GET /orders/{id} (individual, puede ser público). **Validación:** No retorna lista vacía, sino error JSON. **Razón:** Listado de todas las órdenes es sensitive (cocina management). **Mensaje:** "X-Kitchen-Token header is missing". | **Negocio:** Listado completo de órdenes es operacional (cocina access). Debe autenticarse. Individual puede ser público (cliente verificar su propia orden). **Técnica:** Diferentes endpoints, diferentes security policies. |
| **CT-02-013** | **Original:** GET /menu retorna array de MenuItem objects. Cada contiene: id, name, description, price, category, imageUrl, isActive. | **Ajustado:** GET /menu retorna array of MenuItem JSON objects. **Estructura de cada elemento:** `{id: number, name: string, description: string, price: number (decimal), category: string, imageUrl: string or null, isActive: boolean}`. **Validación:** price >= 0 (no negativos), description no null (puede ser ""). **imageUrl:** Puede ser null (si no hay imagen), pero si present debe ser valid URL format. **Consideración:** Si imageUrl es relativa (ej: "/images/empanada.jpg"), validar que es accesible. **Exhaustiveness:** Confirmar que NO incluye campos no documentados (ej: stockLevel, supplier, markupPercentage) - mantener contrato simple. | **Negocio:** Menú es contrato con frontend. Campos consistentes, bien tipados. Frontend depende de estructura. **Técnica:** MenuItem DTO con Jackson annotations @NotNull, @Positive. |
| **CT-02-014** | **Original:** GET /reports respuesta es objeto ReportResponseDTO con estructura: `{totalRevenue: number, orders: array, productSummary: array}`. | **Ajustado:** GET /reports?startDate=...&endDate=... retorna ReportResponseDTO JSON con estructura: `{totalRevenue: decimal (>= 0), orders: array of Order, productSummary: array of {productId, productName, quantity, totalRevenue}}`. **Validación:** totalRevenue es número decimal (ej: 1250.50), no string. **orders:** Array of Order objects (ej: `{orderId, tableId, total, timestamp}`). **productSummary:** Array of {productId (int), productName (string), quantitySold (int), totalRevenue (decimal)}. **Exhaustiveness:** Confirmar que respuesta es completa (no falta información). **Determinism:** Orden de items en arrays (sorted by id? name?). | **Negocio:** Reportes deben ser auditables (números exactos, fechas claras). Estructura fija permite automación (analítica, gráficos). **Técnica:** ReportResponseDTO, nested DTOs para orders y productSummary. |
| **CT-02-015** | **Original:** GET /menu con 1 producto activo. Retorna 200 OK, array contiene exactamente 1 elemento. Elemento es objeto MenuItem válido. | **Ajustado:** GET /menu con exactamente 1 producto activo en BD. Retorna 200 OK, array `[MenuItem]` con 1 elemento. **Validación:** Array length = 1 (not 0, not 2). **Element:** Valida todos los campos de MenuItem (id, name, price, etc.). **Boundary:** Prueba transición: 0 productos (empty []) → 1 producto (array with 1 elem) → 2 productos (array with 2 elems). Confirma que count es correcto. **Performance:** Para 1 elemento, respuesta es rápida. | **Negocio:** Restaurante con 1 ítem en menú (ej: food truck especializado) es válido. API maneja correctamente. **Técnica:** Array serialization, JSON parser validates length. |

---

## Matriz de Ajuste: HDU-03 — Validación de Estructura y Tipo de Datos con 400

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-03-001** | **Original:** JSON con llave sin cerrar `{"tableId": 5, "items"`. Retorna 400 Bad Request. Error menciona problema de formato JSON. No expone traces internos. | **Ajustado:** JSON con llave sin cerrar `{"tableId": 5, "items"` (incomplete, missing closing brace). POST a "/orders". Retorna 400 Bad Request. **Error message:** "Invalid JSON: unexpected end of input" o similar (descriptivo, sin stack trace). **Validación:** HTTP status code = 400 (not 500, not 422). **Response time:** < 100ms (parser falla rápidamente sin BD access). **Content-Type:** "application/json" (error es JSON, no HTML). **Security:** No expone: file paths, Spring package names, Jackson internals. | **Negocio:** JSON malformado es error de protocolo (client mistake, network corruption). Rápido rechazo previne resource exhaustion. **Técnica:** Jackson `JsonParseException`, intercepted en `@ExceptionHandler`, convertido a 400. |
| **CT-03-002** | **Original:** JSON con comilla sin cerrar `{"tableId": 5, "items": [{"productId": "unclosed}]}`. Retorna 400 Bad Request. Mensaje descreve problema de parseo. | **Ajustado:** JSON com quote không closed: `{"tableId": 5, "items": [{"productId": "unclosed}]}` (string productId nunca se cierra). POST a "/orders". Retorna 400 Bad Request. **Message:** "Invalid JSON: unterminated string" (o similar). **Validación:** Status = 400, no 500. Response time < 100ms. **Security:** No stack trace. | **Negocio:** Strings sin cerrar son error de formateo común (copy-paste errors). Parsing falla antes de type checking. **Técnica:** Jackson MappingJsonFactory detecta. |
| **CT-03-003** | **Original:** JSON con caracteres no escapados `{"tableId": 5, "items": [{"note": "Sin "cebolla"}]}` (quote dentro de string no escapada). Retorna 400 Bad Request. | **Ajustado:** JSON con caracteres no escapados. Ejemplo: `{"tableId": 5, "note": "Sin "cebolla"}` (quote interior sin backslash escape). POST a "/orders". Retorna 400 Bad Request. **Validación:** Status 400, message menciona JSON parsing error. **Note:** En JSON, interior quotes deben ser escaped como `\"`. **Ejemplos válidos:** `"Sin \"cebolla\""`. | **Negocio:** JSON RFC 7159 exige escaping de quotes en strings. No hacerlo es error. Early rejection previene parsing errors. **Técnica:** Jackson parser detecta caracteres inesperados. |
| **CT-03-004** | **Original:** POST a "/orders" con cuerpo vacío (body es "" o ausente). Retorna 400 Bad Request. Error menciona que cuerpo es requerido. | **Ajustado:** POST a "/orders" con cuerpo vacío (Content-Length: 0, body is ""). Retorna 400 Bad Request. **Message:** "Request body is missing" o "Request body should not be empty". **Validación:** Spring @RequestBody(required=true) automáticamente rechaza si ausente. **Edge case:** Headers vacíos vs body vacío - ambos retornan 400. **Performance:** < 50ms (validación de entrada). | **Negocio:** Órdenes requieren datos (tableId, items). Body vacío no es válido. **Técnica:** Spring @RequestBody(required=true) default. |
| **CT-03-005** | **Original:** tableId="mesa5" (string en lugar de integer). POST a "/orders" con ese JSON. Retorna 400 Bad Request. Message menciona "tableId must be an integer". NO coerces string a número. | **Ajustado:** tableId="mesa5" (string "mesa5", no número 5). POST a "/orders" with JSON: `{"tableId": "mesa5", "items": [...]}`. Retorna 400 Bad Request. **Message:** "Invalid type for field tableId: expected integer, got string" (o "Cannot deserialize value of type `int` from String"). **Validación:** NO intenta coerción (Jackson strict mode). **Precedent:** Si system config has `DeserializationFeature.ACCEPT_SINGLE_VALUE_AS_ARRAY = true`, ciertos coerciones son posibles. Para robustez, asumir no. **Security:** No intenta SQL injection en string (aunque string no entra en int field, safe by type system). | **Negocio:** Type mismatches son errores claros (string vs int). Cliente debe enviar tipo correcto. **Técnica:** Jackson `@JsonProperty(defaultValue)` si default needed, else fail. |
| **CT-03-006** | **Original:** tableId={} (objeto vacío, no número). POST a "/orders". Retorna 400 Bad Request. | **Ajustado:** tableId={} (object en lugar de integer). POST with JSON: `{"tableId": {}, "items": [...]}`. Retorna 400 Bad Request. **Message:** "Cannot deserialize value of type `int` from START_OBJECT". **Validación:** Status 400 (type mismatch, not 422). **Edge case:** tableId=[] (array) también retorna 400. | **Negocio:** Objetos/arrays en integer fields son definitively wrong. Rápido rechazo. **Técnica:** Jackson type checking. |
| **CT-03-007** | **Original:** items como string "producto1,producto2" (not array). POST a "/orders". Retorna 400 Bad Request. Message menciona "items must be an array". | **Ajustado:** items="producto1,producto2" (string, not array). POST with JSON: `{"tableId": 5, "items": "producto1,producto2"}`. Retorna 400 Bad Request. **Message:** "Cannot deserialize value of type `List` from String". **Validación:** Status 400, no coerction. | **Negocio:** Items es array de objetos (cada tiene productId, quantity, note), no CSV string. **Técnica:** Jackson List deserialization fails on string input. |
| **CT-03-008** | **Original:** productId="uno" (string en lugar de integer) dentro items array. POST a "/orders" con items=[{"productId": "uno", "quantity": 1}]. Retorna 400 Bad Request. Message menciona error de tipo en productId. | **Ajustado:** items=[{"productId": "uno", "quantity": 1}]. productId es string "uno", no integer. POST a "/orders". Retorna 400 Bad Request. **Message:** "Cannot deserialize value of type `int` from String: failed to parse value 'uno' as integer". **Validación:** Error identifies field (productId) y tipo esperado. **Note:** Si productId es string que parece número (ej: "123"), Jackson intenta coercer (puede aceptar). Para robustez, rechazar strings siempre. **Config:** `DeserializationFeature.USE_BIG_DECIMAL_FOR_FLOATS` etc. for strict mode. | **Negocio:** productId debe ser integer (referencia a catálogo por ID numérico). String no acepta. **Técnica:** Jackson @JsonDeserialize(using=StrictIntDeserializer) si needed. |
| **CT-03-009** | **Original:** quantity="dos" (string). POST a "/orders". Retorna 400 Bad Request. | **Ajustado:** items=[{"productId": 1, "quantity": "dos"}]. quantity es string "dos", not integer. POST a "/orders". Retorna 400 Bad Request. **Message:** "Cannot deserialize value of type `int` from String: failed to parse value 'dos' as integer". **Validación:** Status 400, no coerction. | **Negocio:** quantity es número (cantidad de items). String no válido. **Técnica:** Jackson int deserialization. |
| **CT-03-010** | **Original:** quantity es null en items array. POST a "/orders". Retorna 400 Bad Request o 422 Unprocessable Entity. Message menciona "quantity is required". | **Ajustado:** items=[{"productId": 1, "quantity": null}]. quantity es null (missing or explicitly null). POST a "/orders". Retorna 400 Bad Request (if using @NotNull en DTO). **Alternatively:** 422 si regla de negocio (quantity no puede ser null por negocio, no por tipo). **Recomendación:** Use @NotNull y @NotEmpty en DTO para strict validation. **Message:** "Field 'quantity' cannot be null" o similar. | **Negocio:** quantity es obligatorio. null no tiene sentido (¿0 items? usa empty array mejor). **Técnica:** @NotNull en OrderItemDTO quantity field. |
| **CT-03-011** | **Original:** Falta tableId en payload `{"items": [...]}`. POST a "/orders". Retorna 400 Bad Request. Message menciona "tableId is required". | **Ajustado:** JSON `{"items": [{"productId": 1, "quantity": 1}]}` (no tableId). POST a "/orders". Retorna 400 Bad Request. **Message:** "Field 'tableId' is missing" o "Property 'tableId' is required". **Validación:** Spring @NotNull o Jackson @JsonProperty(required=true). **Edge case:** tableId es null (vs missing) - null also rejected by @NotNull. **Message:** Should say "required", not just "null". | **Negocio:** tableId es obligatorio (¿sin mesa, dónde servir?). **Técnica:** @NotNull(message="tableId is required"). |
| **CT-03-012** | **Original:** Falta items en payload `{"tableId": 5}`. POST a "/orders". Retorna 400 Bad Request. Message menciona "items is required". | **Ajustado:** JSON `{"tableId": 5}` (no items). POST a "/orders". Retorna 400 Bad Request. **Message:** "Field 'items' is missing". **Validación:** @NotNull en items. **Edge case:** items=null vs items missing - both rejected. **Consistency:** Message same for both. | **Negocio:** items es obligatorio (¿orden sin items, qué pedir?). **Técnica:** @NotNull(message="items is required"). |
| **CT-03-013** | **Original:** Falta productId en item `{"tableId": 5, "items": [{"quantity": 2}]}`. POST a "/orders". Retorna 400 Bad Request. Message menciona "productId is required in items". | **Ajustado:** items=[{"quantity": 2}] (no productId). POST a "/orders". Retorna 400 Bad Request. **Message:** "Field 'productId' is missing from items[0]". **Mejora:** Message incluye índice del array (items[0]) para claridad. **Validación:** @NotNull en OrderItemDTO productId. | **Negocio:** productId es necesario (¿qué producto pedir sin ID?). **Técnica:** Nested @Valid + @NotNull en orderItem fields. |
| **CT-03-014** | **Original:** Falta quantity en item `{"tableId": 5, "items": [{"productId": 1}]}`. POST a "/orders". Retorna 400 Bad Request o 422. Message menciona "quantity is required in items". | **Ajustado:** items=[{"productId": 1}] (no quantity). POST a "/orders". Retorna 400 Bad Request (si @NotNull). **Message:** "Field 'quantity' is missing from items[0]". **Consideración:** quantity=0 vs missing - ambos inválidos, pero por razones diferentes (0 es semantic error, missing es structural error). Ambos retornan 400/422 pero con mensajes distintos. **Recomendación:** @NotNull para missing, @Min(1) para 0. | **Negocio:** quantity es obligatorio. Validaciones multinivel clarifican diferente errores. **Técnica:** @Valid @NotNull recursión en @Valid annotations. |
| **CT-03-015** | **Original:** note=123 (número en lugar de string). POST a "/orders". Jackson convierte a string o rechaza con 400. Comportamiento depende de configuración de Jackson. | **Ajustado:** items=[{"productId": 1, "quantity": 1, "note": 123}]. note es integer 123, not string. POST a "/orders". **Behavior depends on Jackson config:** (a) COERCE_NUMBER_TO_STRING enabled: convierte 123 → "123" (aceptado). (b) COERCE disabled: rechaza con 400. **Recomendación para MVP:** Disable coercion (strict mode): `DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES = true`, `FAIL_ON_INVALID_SUBTYPE = true`. **Result:** Retorna 400 Bad Request. **Message:** "Cannot deserialize value of type `String` from number". | **Negocio:** notes son texto (instrucciones especiales). Números como notas (123) no tienen sentido. Strict parsing previene confusiones. **Técnica:** Jackson deserialization config, ObjectMapper builder. |
| **CT-03-016** | **Original:** Payload con campos desconocidos `{"tableId": 5, "items": [...], "extraField": "ignorar"}`. POST a "/orders". Retorna 201 Created. extraField es ignorado (no causa error 400). | **Ajustado:** JSON `{"tableId": 5, "items": [...], "extraField": "ignorar", "unknownField": 123}`. POST a "/orders". **Behavior:** Jackson default = ignora campos desconocidos (no falla). **Result:** Retorna 201 Created (extraField ignorado). **Rationale:** Backwards compatibility (si cliente envía campos nuevos, servidor viejo no falla). **Config:** `DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES = false` (default true en Spring 6+, verify). **Test:** Validar que orden se crea sin mencionar unknown fields. | **Negocio:** Unknown fields son permitidos (future-proofing). Cliente puede enviar datos adicionales (API evoluciona). **Técnica:** Jackson default behavior, puede configurarse si needed. |
| **CT-03-017** | **Original:** JSON correctamente formado. POST a "/orders". Retorna 201 Created. Orden creada con UUID. | **Ajustado:** JSON válido: `{"tableId": 5, "items": [{"productId": 1, "quantity": 2, "note": "Sin sal"}]}`. POST a "/orders". Retorna 201 Created. **Validación:** Respuesta contiene nueva orden con id (UUID), status=PENDING, createdAt. **Verifica:** Parsing success, no validation errors, order persisted. **Baseline:** Este test asegura que "happy path" funciona (contrapunto para todos los error tests). | **Negocio:** Happy path debe funcionar. Este test previene regressions. **Técnica:** Integration test con real DB (H2). |
| **CT-03-018** | **Original:** JSON válido con tipos correctos y note=null. POST a "/orders". Retorna 201 Created. | **Ajustado:** JSON: `{"tableId": 5, "items": [{"productId": 1, "quantity": 1, "note": null}]}`. note es null (missing or explicit null). POST a "/orders". Retorna 201 Created. **Validación:** Orden creada. GET /orders/{id} retorna note: null (o note omitido si @JsonInclude(NON_NULL)). **Importante:** null note vs missing note deben tener mismo comportamiento. **Test:** Confirma que null es aceptado (no required). | **Negocio:** notes son opcionales (not all items need special instructions). null es válido. **Técnica:** @NotRequired or @JsonProperty(required=false) en note field. |
| **CT-03-019** | **Original:** JSON válido com 5 items, cada uno con productId, quantity válidos. POST a "/orders". Retorna 201 Created. | **Ajustado:** JSON válido con 5 items (cada uno completo: productId, quantity, opcionalmente note). POST a "/orders". Retorna 201 Created. **Validación:** Response contiene exactly 5 items in items array. **Performance:** Para 5 items, parsing y validación rápidos. **Boundary:** Probar 1, 5, 10, 100 items para ver si hay límites. Si > 100 items rechaza con 422 (límite de negocio), OK. | **Negocio:** Órdenes grandes (100+ items) son raras pero posibles (catering). System debe manejar sin crash. **Técnica:** @Size(max=100) en items list si hay límite. |
| **CT-03-020** | **Original:** Distinguir 400 (formato/tipo) de 422 (negocio). Esquema: 400 = JSON malformado, tipos incorrectos, estructura inválida. 422 = JSON válido pero valores violan reglas de negocio. POST a "/orders". Se retorna 400 si parsing/tipo falla, 422 si entidad es semánticamente inválida. | **Ajustado:** **Distinción clave:** - **400 Bad Request:** JSON unparseable, type mismatch (tableId="string" cuando int esperado), required field missing, structure invalid. - **422 Unprocessable Entity:** JSON válido, tipos correctos, pero valores violan negocio (tableId=13, quantity=-5, items=[], productId inexistente). **Prueba concreta:** - Enviar tableId="mesa" (string) → 400. - Enviar tableId=13 (int, pero out of range) → 422. - Enviar items=[] (valid empty array, pero negocio exige >=1) → 422. **Implementación:** Parsing/type in @ExceptionHandler for Jackson, negocio en service/validator para 422. | **Negocio:** Distinción 400 vs 422 es crítica para frontend (different error handling, user messages). **Técnica:** @ExceptionHandler(HttpMessageNotReadableException) → 400, @ExceptionHandler(MethodArgumentNotValidException) → 422, custom exceptions para reglas de negocio. |
| **CT-03-021** | **Original:** items=[] (JSON válido, array vacío, pero violates "al least 1 item"). POST a "/orders". Retorna 422 Unprocessable Entity (not 400, pues JSON es válido sintácticamente). | **Ajustado:** JSON: `{"tableId": 5, "items": []}`. items es empty array (syntactically valid JSON, pero negocio exige >=1). POST a "/orders". Retorna 422 Unprocessable Entity. **Message:** "Order must contain at least one item. Please select from menu." **Validación:** @NotEmpty o @Size(min=1) en items list → Spring validation falla → 422. **Importante:** NO es 400 (que sería si JSON malformado). **Evidence:** Si system retorna 400 for items=[], it's a bug (mixing parse errors with business validation). | **Negocio:** Validación de "tamaño mínimo" es regla de negocio, no sintaxis. **Técnica:** @NotEmpty or @Size(min=1) en DTO → ConstraintViolationException → 422. |

