# Matriz de Pruebas

---

## HDU-01 — Respuesta 201 Created con Header Location

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-01-001** | Crear orden válida con 1 item. Retorna → 201 Created. | Crear orden válida con 1 item. Retorna → 201 Created con header `Location` presente y formato UUID v4 verificado. | Confirmar que el header Location no solo existe sino que apunta a un recurso con UUID válido según RFC 4122. |
| **CT-01-002** | Crear orden válida con múltiples items. Retorna → 201 Created. | Crear orden con 3 items válidos. Retorna → 201 Created con header `Location` apuntando a UUID válido. Verificar que los 3 items se persistan correctamente en DB. | Un cliente puede pedir varios productos a la vez. El test confirma que todos los items se guardan sin perder ninguno. |
| **CT-01-003** | Crear orden en mesa al límite superior (tableId=12). Retorna → 201 Created. | Crear orden con tableId=12. Retorna → 201 Created. Verificar que el evento RabbitMQ se publique correctamente y la orden persista con tableId=12. | Mesa 12 es el límite superior permitido. El test asegura que valores al límite se acepten sin error. |
| **CT-01-004** | Crear orden en mesa al límite inferior (tableId=1). Retorna → 201 Created. | Crear orden con tableId=1. Retorna → 201 Created. Verificar que la orden se guarde correctamente en restaurant_db. | Mesa 1 es el límite inferior permitido. El test asegura que el valor mínimo sea válido. |
| **CT-01-005** | Orden con nota en item. Retorna → 201 Created. | Crear orden con item que incluye nota="Sin lácteos". Retorna → 201 Created. Verificar que la nota aparezca tanto en la respuesta como en el evento RabbitMQ. | Los clientes pueden pedir personalizaciones. El test confirma que las notas se transmitan correctamente hasta la cocina. |
| **CT-01-006** | tableId fuera de rango superior (13). Retorna → 400 o 422. | tableId=13 excede el máximo permitido. Retorna → 400 Bad Request o 422 Unprocessable Entity. Verificar que NO se publique evento RabbitMQ ni se persista la orden. | Solo hay 12 mesas en el restaurante. Si el sistema acepta mesa 13, el pedido no tendrá destino físico. |
| **CT-01-007** | tableId fuera de rango inferior (0). Retorna → 400 o 422. | tableId=0 es menor que el mínimo (1). Retorna → 400 o 422. Verificar que la orden no se cree. | No existe mesa 0 en el negocio. El sistema debe rechazar este valor inmediatamente. |
| **CT-01-008** | tableId negativo (-5). Retorna → 400 o 422. | tableId=-5 es inválido. Retorna → 400 o 422 con mensaje descriptivo sobre el rango válido. | Un número negativo no tiene sentido para identificar mesas. El error debe ser claro para que el cliente corrija. |
| **CT-01-009** | Sin items en la orden. Retorna → 400 o 422. | Array items vacío `[]`. Retorna → 400 o 422. El error debe mencionar que se requiere al menos 1 item. | Una orden sin productos no tiene sentido en un restaurante. El sistema debe rechazarla de inmediato. |
| **CT-01-010** | productId que no existe. Retorna → 400 o 422. | productId=999 no existe en menu_items. Retorna → 400 o 422. El error especifica que el producto no existe. | Si el sistema acepta IDs inventados, la cocina no sabrá qué preparar y el pedido quedará en limbo. |
| **CT-01-011** | productId inactivo (is_active=false). Retorna → 400 o 422. | Crear orden con productId=2 donde is_active=false. Retorna → 400 o 422. El error menciona que el producto no está disponible. | Si un producto está desactivado (agotado, fuera de menú), no debe poder pedirse. Aceptarlo generaría expectativas falsas. |
| **CT-01-012** | quantity=0. Retorna → 400 o 422. | quantity=0 en un item. Retorna → 400 o 422. El error menciona que la cantidad debe ser positiva. | Pedir 0 unidades de algo no tiene sentido. El sistema debe rechazar este valor. |
| **CT-01-013** | quantity negativa (-3). Retorna → 400 o 422. | quantity=-3 es inválida. Retorna → 400 o 422 con mensaje descriptivo. | Una cantidad negativa es absurda para un pedido. El error debe indicar claramente el problema. |
| **CT-01-014** | tableId como string en lugar de número. Retorna → 400. | tableId="mesa5" (texto en lugar de número). Retorna → 400 Bad Request. El error indica problema de tipo de dato. | El backend espera un número. Si acepta strings sin quejarse, el cliente enviará datos mal formados sin saberlo. |
| **CT-01-015** | JSON malformado (llave sin cerrar). Retorna → 400. | JSON con formato inválido: `{"tableId": 5, "items"`. Retorna → 400 Bad Request sin exponer detalles internos del parser. | Un JSON roto es error del cliente. El mensaje debe ser útil sin revelar cómo funciona internamente el servidor. |
| **CT-01-016** | Orden con máxima cantidad en un item (largeNumber). Retorna → 201 Created. | quantity=999999 en un item. Retorna → 201 Created. Verificar que la orden persista con esa cantidad exacta. | Sin límite superior explícito en reglas de negocio, el sistema debe aceptar cantidades grandes por si un cliente hace pedidos al por mayor. |
| **CT-01-017** | Orden con múltiples items del mismo producto. Retorna → 201 Created. | items=[{productId: 1, qty: 2}, {productId: 1, qty: 3}]. Retorna → 201 Created. Verificar que ambos items se persistan por separado sin consolidarse. | El cliente puede querer el mismo producto con notas diferentes. El sistema no debe fusionarlos automáticamente. |
| **CT-01-018** | Nota vacía en item. Retorna → 201 Created. | nota="" (cadena vacía). Retorna → 201 Created. Verificar que se guarde como null o "". | Una nota vacía es equivalente a sin nota. El sistema debe manejarla sin fallar. |
| **CT-01-019** | Nota con caracteres especiales. Retorna → 201 Created. | nota="¡Sin sal, pimentón! & <>". Retorna → 201 Created. Verificar que se persista exactamente igual, escapada si es necesario. | Los clientes pueden escribir con símbolos o tildes. El sistema debe guardar el texto tal cual sin corromperlo. |
| **CT-01-020** | Header Location con UUID v4 válido. Retorna → 201 Created. | Examinar header `Location` de orden creada. Retorna → 201 Created con formato "http://localhost:8080/orders/{UUID-v4}" válido según RFC 4122. | El header Location debe ser una URL completa y funcional para que el cliente pueda consultar el recurso creado. |
| **CT-01-021** | Evento order.placed con eventVersion=1. Retorna → 201 Created. | Crear orden válida y verificar evento RabbitMQ. Retorna → 201 Created. El evento debe tener eventVersion=1 (no 2, no "1", no null). | El kitchen-worker solo entiende eventos versión 1. Cualquier otra versión va directamente a DLQ sin procesarse. |
| **CT-01-022** | Orden duplicada retorna 201 con diferente UUID. Retorna → 201 Created. | Enviar el mismo payload dos veces. Retorna → 201 Created en ambas con UUIDs DIFERENTES. Verificar que hay DOS órdenes en DB. | El sistema no implementa idempotencia de cliente. Cada petición genera una orden nueva, incluso si el payload es idéntico. |
| **CT-01-023** | Evento publicado antes de que GET devuelva 201. Retorna → 201 Created. | Crear orden y consultar GET /orders/{id} inmediatamente. Retorna → 201 Created. Verificar que el evento ya exista en RabbitMQ y la orden en restaurant_db. | La orden debe ser visible tanto en la DB como en el sistema de eventos para asegurar consistencia inmediata. |
| **CT-01-024** | Header Content-Type en respuesta 201. Retorna → 201 Created. | Crear orden y verificar headers. Retorna → 201 Created con Content-Type: application/json. | El cliente necesita saber que la respuesta es JSON para parsearla correctamente. |
| **CT-01-025** | Cuerpo de respuesta 201 contiene createdAt y updatedAt. Retorna → 201 Created. | Crear orden y analizar respuesta. Retorna → 201 Created con campos id, tableId, status, items, createdAt, updatedAt. | El cliente necesita timestamps para rastrear cuándo se creó y actualizó la orden. |
| **CT-01-026** | GET a recurso creado retorna 200 OK sin autenticación. Retorna → 200 OK. | Crear orden, obtener Location header y hacer GET sin token. Retorna → 200 OK con información completa de la orden. | El endpoint GET /orders/{id} es público. Cualquiera puede consultar una orden conociendo su UUID. |
| **CT-01-027** | GET a recurso creado con X-Kitchen-Token retorna 200 OK. Retorna → 200 OK. | Crear orden y hacer GET con header X-Kitchen-Token. Retorna → 200 OK con orden completa en estado PENDING. | La cocina necesita consultar órdenes usando su token. El sistema debe permitirlo sin restricciones. |
| **CT-01-028** | Orden recién creada tiene estado PENDING en GET. Retorna → 200 OK. | Crear orden y consultarla vía GET. Retorna → 200 OK con status="PENDING". | Toda orden nueva comienza en PENDING. Este estado no puede ser modificado hasta que la cocina la procese. |

---

## HDU-02 — Respuesta 200 OK con Lista Vacía

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-02-001** | GET /menu retorna 200 OK con array vacío cuando no hay productos. Retorna → 200 OK. | GET /menu cuando la DB está vacía o todos los productos tienen is_active=false. Retorna → 200 OK con cuerpo exactamente `[]`, NO null. | Un menú vacío es un estado válido del negocio (por ejemplo, si todo se agotó). Enviar lista vacía en lugar de error evita que la app colapse. |
| **CT-02-002** | GET /menu retorna 200 OK con productos cuando existen. Retorna → 200 OK. | GET /menu con 3 productos activos en DB. Retorna → 200 OK con array de 3 elementos. Cada elemento contiene: id, name, description, price, category, imageUrl, isActive. | Este es el caso normal de operación. El test confirma que cuando hay datos, se devuelven correctamente formateados. |
| **CT-02-003** | GET /menu filtra SOLO productos con is_active=true. Retorna → 200 OK. | GET /menu con 5 productos en total (3 activos, 2 inactivos). Retorna → 200 OK con array de exactamente 3 productos. | Los productos desactivados no deben aparecer en el menú. Esto evita que se pidan items que ya no están disponibles. |
| **CT-02-004** | GET /reports con rango sin órdenes READY retorna 200 OK con array vacío. Retorna → 200 OK. | GET /reports para rango sin órdenes READY. Retorna → 200 OK con totalRevenue=0.0, orders=[], productSummary=[]. | Un período sin ventas es posible (por ejemplo, el restaurante cerrado). Enviar ceros en lugar de error permite que los reportes se muestren sin problemas. |
| **CT-02-005** | GET /reports retorna totalRevenue=0 cuando no hay ventas. Retorna → 200 OK. | GET /reports sin órdenes READY en el período. Retorna → 200 OK con totalRevenue exactamente 0 o 0.0 y productSummary=[]. | Sin ventas, los ingresos son cero. El sistema debe calcularlo explícitamente en lugar de fallar. |
| **CT-02-006** | GET /reports con rango futuro sin datos. Retorna → 200 OK. | GET /reports para fechas futuras (2099-01-01 a 2099-12-31). Retorna → 200 OK con todos los conteos y sumatorias en 0. | Consultar fechas futuras no debe romper el sistema. Debe devolver valores iniciales coherentes. |
| **CT-02-007** | GET /orders retorna 200 OK con array vacío cuando no hay órdenes. Retorna → 200 OK. | GET /orders cuando la tabla orders está vacía o todas tienen deleted=true. Retorna → 200 OK con cuerpo exactamente `[]`. | Sin órdenes activas, la lista está vacía. Esto es válido y no debe confundirse con un error del servidor. |
| **CT-02-008** | GET /orders?status=PENDING retorna 200 OK con array vacío. Retorna → 200 OK. | GET /orders?status=PENDING sin órdenes en ese estado. Retorna → 200 OK con array vacío `[]` sin incluir órdenes de otros estados. | Filtrar por estado y no encontrar resultados es un caso normal. El sistema debe devolver lista vacía sin contaminar con otros estados. |
| **CT-02-009** | GET /menu sin autenticación retorna 200 OK (público). Retorna → 200 OK. | GET /menu sin header X-Kitchen-Token. Retorna → 200 OK con menú completo. | El menú es público. Cualquier cliente o visitante puede consultarlo sin necesidad de autenticarse. |
| **CT-02-010** | GET /reports sin X-Kitchen-Token retorna 401 Unauthorized. Retorna → 401. | GET /reports sin header X-Kitchen-Token. Retorna → 401 Unauthorized con error JSON estandarizado mencionando que se requiere autenticación. | Los reportes son información sensible del negocio. Solo personal autorizado (cocina/admin) debe poder consultarlos. |
| **CT-02-011** | GET /reports con X-Kitchen-Token inválido retorna 401. Retorna → 401. | GET /reports con X-Kitchen-Token="invalid_token_xyz". Retorna → 401 Unauthorized sin revelar detalles sobre por qué el token es inválido. | Un token falso no debe dar acceso. Ocultar el motivo del rechazo evita dar pistas a atacantes que prueban diferentes tokens. |
| **CT-02-012** | GET /orders sin X-Kitchen-Token pero con status=PENDING. Retorna → 401. | GET /orders?status=PENDING sin X-Kitchen-Token. Retorna → 401 Unauthorized sin devolver lista (vacía o no). | Consultar órdenes por estado es una operación de cocina. Requiere autenticación para evitar que cualquiera espíe los pedidos. |
| **CT-02-013** | Respuesta de /menu es un array de objetos MenuItem. Retorna → 200 OK. | GET /menu y verificar estructura. Retorna → 200 OK con Content-Type: application/json. Cada elemento tiene: id, name, description, price, category, imageUrl, isActive. | Mantener el contrato fijo del API evita que el frontend se rompa si falta algún campo. |
| **CT-02-014** | Respuesta de /reports es un objeto ReportResponseDTO con estructura fija. Retorna → 200 OK. | GET /reports con parámetros válidos. Retorna → 200 OK con estructura: {totalRevenue: number, orders: array, productSummary: array}. | La estructura fija del reporte permite que el frontend lo parsee sin sorpresas ni errores. |
| **CT-02-015** | Lista con 1 elemento retorna array con 1 elemento (no vacío). Retorna → 200 OK. | GET /menu con exactamente 1 producto activo. Retorna → 200 OK con array de 1 elemento válido. | Confirmar que listas con un solo elemento no se confundan con casos vacíos o errores. |

---

## HDU-03 — Validación de Estructura y Tipo de Datos con 400

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-03-001** | JSON con llave sin cerrar. Retorna → 400. | JSON con llave sin cerrar sin exponer stack traces internos (Jackson/Spring). Retorna → 400. | Un JSON malformado es error del cliente. El mensaje debe ser accionable sin revelar internals del parser. |
| **CT-03-002** | JSON con comilla sin cerrar. Retorna → 400. | JSON con comilla sin cerrar y sin exponer nombres de clases internas. Retorna → 400. | Si el cliente envió JSON inválido, el mensaje debe guiar sin filtrar detalles del parser. |
| **CT-03-003** | JSON con caracteres no escapados. Retorna → 400. | JSON con caracteres no escapados. Retorna → 400 capturado por @ExceptionHandler. | El ajuste confirma que este error es capturado por el @ExceptionHandler global y devuelve ErrorResponse estándar, en lugar de una respuesta inconsistente o HTML. |
| **CT-03-004** | Cuerpo vacío "" o sin body en POST /orders. Retorna → 400. | Cuerpo vacío "" o sin body en POST /orders. Retorna → 400. Probar body "" y request sin body por separado. | Sin payload no hay orden. El defecto debe distinguir "body vacío" de "body malformado". |
| **CT-03-005** | tableId como string "mesa5". Retorna → 400. | tableId como string "mesa5". Retorna → 400. Verificar que Jackson no convierta silenciosamente el string a número. | Si el sistema acepta strings donde espera números sin quejarse, el cliente nunca sabrá que está enviando datos mal tipados, lo que podría causar bugs difíciles de seguir. |
| **CT-03-006** | tableId como objeto {}. Retorna → 400. | tableId como objeto {}. Retorna → 400 con ErrorResponse. | Lo que se puede comprobar es que intentar pasar un objeto {} donde se espera un número simple falla inmediata, explícita y con el formato estándar de error, no con un 500 ni con HTML. |
| **CT-03-007** | items como string "producto1,producto2". Retorna → 400. | items como string "producto1,producto2". Retorna → 400. El error indica que items debe ser un array. | El servidor rechaza el CSV porque espera un array JSON y el error es suficientemente informativo como para que el cliente pueda corregir. |
| **CT-03-008** | productId como string en lugar de number. Retorna → 400. | productId como string en lugar de number. Retorna → 400. El error indica que el tipo de dato no es el correcto. | El mensaje debe señalar el campo y el tipo esperado para que el cliente sepa que debe corregir. |
| **CT-03-009** | quantity como string en lugar de number. Retorna → 400. | quantity como string en lugar de number. Retorna → 400 indicando tipo incorrecto. | quantity debe ser numérico para cálculos, por eso el error debe ser preciso sobre el campo fallido. |
| **CT-03-010** | quantity como null. Retorna → 400 o 422. | quantity como null. Retorna → 400 o 422. El error menciona que quantity es requerida. | Si el cliente envía null, el servidor debe fallar diciendo "quantity es requerido", evitando así crear órdenes fantasmas con 0 unidades. |
| **CT-03-011** | Falta tableId en payload. Retorna → 400. | Falta tableId en payload. Retorna → 400. El error menciona que tableId es requerido. | Sin tableId no se sabe a dónde llevar el pedido, por eso el mensaje debe indicar el campo ausente. |
| **CT-03-012** | Falta items en payload. Retorna → 400. | Falta items en payload. Retorna → 400. El error menciona que items es requerido. | Una orden sin items es inválida, por eso el mensaje debe informar sobre un faltante total de items o una lista vacía. |
| **CT-03-013** | Falta productId en un item. Retorna → 400. | Falta productId en un item. Retorna → 400. El error indica que productId es requerido. | Sin productId no se sabe qué producto ordenar, por eso el mensaje debe indicar el campo faltante exacto dentro del arreglo. |
| **CT-03-014** | Falta quantity en un item. Retorna → 400 o 422. | Falta quantity en un item. Retorna → 400 o 422. El mensaje menciona que quantity es requerido. | La falta de cantidad no debe asumirse nunca como un 1 por defecto, debe ser rechazada explícitamente obligando al cliente a definirla. |
| **CT-03-015** | note como número 123 en lugar de string. Retorna → 201. | Ningún ajuste adicional. | - |
| **CT-03-016** | Payload con campo adicional "extraField". Retorna → 201. | Payload con campo adicional "extraField". Retorna → 201. Verificar que dicho campo extra es ignorado por completo. | Jackson ignora campos desconocidos por defecto. Este test confirma que la persistencia en DB y la respuesta hacia el cliente no filtran ni muestran esa basura extra. |
| **CT-03-017** | JSON válido con tipos correctos (flujo feliz). Retorna → 201. | JSON válido con tipos correctos. Retorna → 201 con un UUID v4 asignado. Verificar que se persistan bien todos sus campos. | Se asegura de que las operaciones ideales de venta realmente se almacenen como se deben. |
| **CT-03-018** | JSON válido con note: null. Retorna → 201. | JSON válido con note: null. Retorna → 201. Verificar que note se quede como un valor nulo de datos sin serializarse accidentalmente al string "null". | Una nota nula es equivalente a sin nota, enviar un null directamente debe parsearse bien y debe cuidarse de que una librería lo acabe metiendo de forma textual en la DB. |
| **CT-03-019** | JSON con 5 items válidos. Retorna → 201. | JSON con 5 items válidos. Retorna → 201. Verificar que los 5 elementos existan tanto en persistencia como al retornar respuestas validativas. | Comprar varios elementos a la vez compone el escenario normal de todo negocio, ratificando la robustez serial del endpoint a listados. |
| **CT-03-020** | Distinción entre 400 y 422. Retorna → 400 / 422. | Diferenciar errores en el documento. Retorna → 400 para malformaciones nativas JSON y Retorna → 422 en caso de reglas lógicas del negocio violadas. | Si el cliente que consume los apis no conoce la naturaleza de su falla, no sabrá si arregla el json crudo en el frontend o arregla los limitantes del negocio. |
| **CT-03-021** | Arreglo limpio items: []. Retorna → 422. | Arreglo limpio items: []. Retorna → 422. El mensaje le indicará textualmente la obligación mínima "al menos un item requerido". | Pasarlo con formato vacío respeta a Java pero lógicamente arruina al restaurante, requerir datos not-null de esta forma impone sentido al ciclo de negocios. |

---

## HDU-04 — Desactivación Lógica de Productos con Auditoría

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-04-001** | Desactivar producto activo (id=1). Retorna → 200 OK. | Desactivar producto activo (id=1). Retorna → 200 OK con metadata auditable, verificando estricto uso del dato `updatedBy`. | Las desactivaciones impunes pierden rápidamente trazabilidad. Saber quien desactivó el artículo y hasta qué tiempo se lo desactivó ofrece una garantía formal de trazabilidad interesante en la gerencia.|
| **CT-04-002** | Desactivar producto activo (id=1). Retorna → 200 OK y verifica que fechas no cambian. | Desactivar producto activo (id=1). Retorna → 200 OK confirmando la inmutabilidad histórica del timestamp primario `createdAt`. | El timestamp funde trazabilidad a la base de datos. Si sobre-escribimos por actualizar arruinariamos la línea total temporal original de registro. |
| **CT-04-003** | Desactivar producto y verificar GET /menu no lo trae. Retorna → 200 OK. | Desactivar producto y correr listado. Retorna → 200 OK ratificando por el conteo la ausencia total de identificadores pasados al filtro. | Elementos extintos en visibilidad confunden ordenadores y cocineros posibilitando demandas inexistentes de comida, provocando así caídas en el servicio. |
| **CT-04-004** | Desactivar producto ya inactivo (id=2). Retorna → 200 OK. | Ningun cambio adicional |- |
| **CT-04-005** | Múltiples DELETE al mismo producto. Retorna → 200 OK. | Ningun cambio adicional | - |
| **CT-04-006** | DELETE a producto 999. Retorna → 404. | DELETE a producto 999. Retorna → 404 Not Found con mensaje que avisa estrictamente no se encontró índice exacto inyectado en path del objeto de fallback estándar. | Carencias de items irreales no quiebran reglas en un java sino al contrato de la api. |
| **CT-04-007** | DELETE al identificador -1. Retorna → 404. | DELETE al identificador -1. Retorna → 404 Not Found previniendo interacciones nocivas de un dígito incalculable. | Si no se verifica que los números enviados tengan sentido (por ejemplo, no aceptar un ID negativo), los errores pueden llegar a la base de datos e incluso eliminar datos incorrectos por error. |
| **CT-04-008** | DELETE con nulos y límite 0. Retorna → 404. | Ningun cambio adicional | - |
| **CT-04-009** | DELETE sin Header. Retorna → 401. | DELETE evadiendo todo el nivel Header Auth. Retorna → 401 Unauthorized sin ninguna posibilidad a borrar o acceder a transacciones. | Si no exigimos que la persona esté autenticada para poder eliminar algo, cualquier usuario malintencionado podría borrar la información desde internet. |
| **CT-04-010** | DELETE con firmamento dañado de token. Retorna → 401. | Tratar exclusiones asomando tokens desbordados. Retorna → 401 Unauthorized ocultándole por completo el diagnóstico de token viciado u otro a la contraparte del llamado. | Guardar total silencio contra ataques inhabilitadores neutraliza el mapeado directo a cibercriminales quienes prueban rutinas hasta acertar mediante pautas informadas. |
| **CT-04-011** | DELETE sin rol superior. Retorna → 403. | Ningun cambio adicional | - |
| **CT-04-012** | Respuesta OK incluye información base. Retorna → 200 OK. | Ningun cambio adicional |-|
| **CT-04-013** | Respuesta 404 retorna JSON estandarizado de error. Retorna → error 404 estándar. |Ningun cambio adicional | Si el sistema devuelve estructuras de error diferentes para un mismo fallo, la interfaz web (Frontend) se confundirá y no podrá mostrarlos correctamente al usuario. |
| **CT-04-014** | Suspender recurso en lazo vivo tipo pedido `PENDING`. Retorna → 200 OK. | Bloqueo a productos. Retorna → 200 OK e ignoramiento cascada, confirmando que transaccionados pedidos históricos se aislen y jamás corrompan órdenes previas pagadas. | Si un cliente ya pidió un producto y la orden está pendiente o pagada, desactivar el producto después no debería alterar ni borrar esa orden de la base de datos. |
| **CT-04-015** | Supresión genérica sobre totalidad base . Retorna → Arreglos nulos 200. |Supresión genérica sobre totalidad base . Retorna → Arreglos nulos 200 enviando al listado un arreglo netamente de corchetes vacíos obviando interrupciones por vacíos aparentes 404 . | Un menú vacío es un estado válido (por ejemplo, si se agotó todo para la venta). Enviar una lista vacía en lugar de un error evita que la página o app colapse repentinamente. |
| **CT-04-016** | Crear la orden referenciando anulados formalmente. Retorna → error. | Mandar orden con producto anulado . Retorna → 400 o 422 y aborta consolidar pago demostrando negatiemientos al inactivo id del alimento en la pasarela. | El sistema debe impedir que se pidan productos que ya no están disponibles en la carta.  |

---

## HDU-05 — Respuestas de Error JSON Estandarizadas (Desarrollador)

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-05-001** | Validación fallida: `tableId` fuera de rango. Retorna → 400. | Validación limitando de 0 a 13. Retorna → 400 emitiendo ErrorResponse que informa esclarecedoramente que solo existen de la 1 a la 12 como tope real válido . | Guiar hacia números permitidos suprime confusiones al instante por una simple vista garantizando reincorporaciones exitosas. |
| **CT-05-002** | Validación fallida: sin items. Retorna → 400. | Ningun cambio adicional | - |
| **CT-05-003** | Validación fallida: producto inexistente. Retorna → 400. | Validación de producto 999. Retorna → 400 detallando el ítem omitido en la DB . | Desfases constantes temporales en app propician requerimientos a elementos vacíos, comunicar ese ID agilizará correcitvas operacionales en piso directo por la cocina . |
| **CT-05-004** | JSON malformado. Retorna → 400. | JSON amputados o ilegibles al ojo nativo java . Retorna → 400 garantizando de modo perentorio envoltorio total al clásico Html nativo blando del servidor spring y encadenando un JSON robusto . | Exhibir fallas sucias al parsear devela códigos fuentes a escudriñadores expertos maliciosos forzando visiones de base técnica. |
| **CT-05-005** | GET /orders obviando header auth pre-designado. Retorna → 401. | Ningun cambio adicional | - |
| **CT-05-006** | GET /reports acarreando firma de token inválido. Retorna → 401. | GET bajo token nulo destrozado o falso. Retorna → 401 Unauthorized mimetizado sin dar avisos sobre si expiro tecnicmente o de hecho es completamente ilicito . | Enmascarar las bases del rechazo evita obsequios sobre información puntual la cual atacantes evalúan automatizadamente . |
| **CT-05-007** | GET order apuntando UUID no localizable . Retorna → 404. | Búsqueda direccionando ID corrompido global o extraviado históricamente. Retorna → 404 Not Found. | Al estampar directamente ID extraviados al body logeable facilita revisiones a profundidad evitando retrasar tiempos de escrutinio log local correlacional. |
| **CT-05-008** | GET hacia items no existentes externamente. Retorna → 404. | Referenciar item que no reside base temporal global actual . Retorna → 404 Not Found replicando de base la estructura formal del error genérico original amoldándose para homologar reportes locales enteros . | Unificar estructura final a cualquier 404 indistintamente su ruta inicial simplifica rutinas receptoras UI centralizadamente en interfaces cliente web app. |
| **CT-05-009** | Report-service al caerse su conexión. Retorna → 500. | Corte directo con proveedor DB. Retorna → 500 cegando el error con caretas que esconden la dirección web, credencial privada o puertos enlazados de infraestructura interna . | Liberar variables físicas sobre topologías activas es regalar mapas vitales a piratería web , incurriendo falta grave según la norma general OWASP referida a entornos protegidos cerrados. |
| **CT-05-010** | Order-service experimentando Excepciones sin mapear global. Retorna → 500. | Cortar servicios al simular hilos caídos de java puros . Retorna → 500 atrapado a un asesor en `@ControllerAdvice` reestructurando el `text/html` no querido sobre Json nativo puro formal y cerrado . | Atajar nativamente excepciones ciegas impide que tomcats nativos o springs tiren pantallas en blanco al usuario limitando severas incomprensiones UX operativas para el comedor del día . |
| **CT-05-011** | Simetría estructural evaluable en sistema. Retorna → 400. | Inyectar igual fallo sobre 2 servicios (orden - reporte). Retorna → 400 evaluando asimetrías para dictaminar congruencias puras en formato, estructura exactita, tipados idénticos logrando simetría paralela. | Requerir paralelismo puro exime tiempo perdido en frontend programando parseos desiguales innecesarios, fortaleciendo flujos simples y directos ante los llamados api generales . |
| **CT-05-012** | Confirmar formato `content-type`. Retorna → application/json. | Interrogar fallos mixtos evaluando la procedencia cabecera constante. Retorna → headers de naturaleza `application/json` limitando al estricto parseo final. | Ignorancia del factor type condena al fracaso todo procesador externo de clientes web quienes basan sus integradores en el type JSON esperado truncando los fallbacks. |
| **CT-05-013** | Validaciones superpuestas de choque al tiempo. Retorna → 400. | Ejecutar llamadas sin mesa y datos puros `[]`. Retorna → 400 estableciendo normativas bases si evalua fail fast u ofrece la completitud formal dictaminada en contrato base resolutivo conjunto. | Formalizar normativas conjuntas disipa las resoluciones continuadas tediosas ofreciéndole al visualizador cliente opciones unidas conjuntas para que sane asimetrías sin recargas extra en api. |
| **CT-05-014** | Errores tolerantes a inyecciones. Retorna → Error estándar. | Infiltrar adjuntos extras al array del control `ErrorResponse`. Retorna → JSON estable evitando quiebres nativos asimilando a todo momento factores requeribles. | Aceptar adjuntos progresivos permite crecimientos maduros sobre reportes profundos en backend que no arruinan módulos débiles desactualizados al conservar compatibles. |
| **CT-05-015** | Transición asíncrona ilógica estado vivo. Retorna → 409. | Enviar parche evaluativo estado preparativo PENDING a READY. Retorna → 409 . | El freno impuesto garantiza fluidez oficial sin manipuleo o atajos perjudiciales operativos sobre cocina base final validante . |

---

## HDU-07 — Validación de Máquina de Estados en Transiciones de Órdenes

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-07-001** | Transición válida PENDING → IN_PREPARATION. Retorna → 200 OK. | Verificar que el estado persiste correctamente en DB tras el PATCH y que la respuesta incluye el nuevo estado. Retorna → 200 OK. | Confirmar que la actualización es efectiva tanto en la respuesta como en persistencia, no solo en memoria. |
| **CT-07-002** | Salto de estado PENDING → READY (saltando IN_PREPARATION). Retorna → 409 Conflict. | PATCH a READY desde PENDING. Retorna → 409 Conflict con mensaje descriptivo indicando la transición inválida. | La máquina de estados no permite saltos. El error debe ser claro para que el operador de cocina entienda el flujo correcto. |
| **CT-07-003** | Transición hacia atrás IN_PREPARATION → PENDING. Retorna → 409 Conflict. | PATCH a PENDING desde IN_PREPARATION. Retorna → 409 Conflict. El estado NO debe revertirse. | Retroceder en el flujo rompe la trazabilidad del pedido. El sistema debe rechazarlo explícitamente. |
| **CT-07-004** | PATCH sin X-Kitchen-Token. Retorna → 401 Unauthorized. | Ningún cambio adicional. | Solo el personal de cocina autenticado puede cambiar estados de órdenes. |
| **CT-07-005** | PATCH sobre orden inexistente. Retorna → 404 Not Found. | UUID aleatorio no existente en DB. Retorna → 404 con JSON estándar. | Una orden que no existe no puede actualizarse. El error debe ser claro y estandarizado. |

### Revisión humana
> ⚠️ **Conflicto de código HTTP con HDU-08:** Los casos CT-07-002 y CT-07-003 retornan `409 Conflict` para transiciones de estado inválidas, mientras que HDU-08 (CT-08-002) usa `422 Unprocessable Entity` para el mismo tipo de error. Esto genera dos rutas de manejo de error en el frontend para la misma excepción de negocio, aumentando la complejidad y el riesgo de bugs. **Se recomienda unificar ambas historias en un único código HTTP** — `409 Conflict` es semánticamente más preciso para violaciones de máquina de estados.
>
> 💡 **Mejora:** El CT-05-015 en HDU-05 ya cubre parcialmente este escenario (transición PENDING → READY). Sería conveniente consolidar estos casos en la sección HDU-07 para evitar duplicación de cobertura entre matrices.

---

## HDU-08 — Respuesta 422 Unprocessable Entity para Errores de Validación de Negocio

| ID | Caso Original (Gema B) | Ajuste Realizado por el Probador | ¿Por qué se ajustó? |
|---|---|---|---|
| **CT-08-001** | Crear orden con tableId mayor a 12. Retorna → 422 Unprocessable Entity. | POST /orders con tableId=13. Retorna → 422 con mensaje indicando rango válido (1-12). Verificar que no se persista la orden ni se publique evento RabbitMQ. | La validación de negocio debe impedir que el sistema acepte datos fuera del contrato antes de cualquier procesamiento. |
| **CT-08-002** | Transición de estado inválida READY → IN_PREPARATION. Retorna → 422 Unprocessable Entity. | PATCH de READY a IN_PREPARATION. Retorna → 422 con mensaje indicando que el estado no puede retroceder. | Esta transición viola las reglas de negocio de la máquina de estados. El error debe ser explícito y consistente. |
| **CT-08-003** | JSON malformado para confirmar distinción 400 vs 422. Retorna → 400 Bad Request. | POST con JSON que tiene una llave sin cerrar. Retorna → 400 (no 422), confirmando que el formato roto es error técnico, no de negocio. | La distinción 400/422 es clave para el frontend: 400 = corregir el JSON; 422 = corregir la lógica de negocio enviada. |
| **CT-08-004** | Crear orden con items vacíos `[]`. Retorna → 422. | POST con items=[]. Retorna → 422 con mensaje "se requiere al menos un ítem". | Una orden sin ítems es inválida por regla de negocio, no por formato. Debe retornar 422 para que el frontend distinga el tipo de error. |

### Revisión humana
> ⚠️ **Conflicto de código HTTP con HDU-07:** CT-08-002 retorna `422 Unprocessable Entity` para una transición de estado inválida (`READY → IN_PREPARATION`), mientras que HDU-07 retorna `409 Conflict` para el mismo tipo de fallo. Mantener dos códigos HTTP distintos para la misma excepción de negocio obliga al cliente a implementar dos handlers separados sin razón, generando complejidad innecesaria y posibles bugs cuando uno de los dos no esté cubierto. **Se recomienda consolidar en un único código HTTP para toda la matriz de pruebas** — idealmente `409 Conflict`.
>
> 💡 **Mejora de alcance:** CT-08-001 (validación de `tableId`) solapa con casos ya cubiertos en HDU-03 y HDU-05. Revisar si este caso debe mantenerse aquí o unificarse en la sección correspondiente para evitar duplicación y conflictos de criterio entre el Probador y la IA.
